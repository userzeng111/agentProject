---
name: 前后端全链路打回复测
description: 通过实际运行的前后端服务，对大纲审核、章节审核、全文验证执行一次“先打回后通过”的完整链路复测
type: project
---

# 前后端全链路打回复测

## 用户原始诉求
由代理自行进行审核测试，要求前后端一起验证，并且在整体链路中：
- 大纲需要先打回重审一次
- 正文审核需要先打回重审一次
- 整体验证链路也需要实际走通

## 当前已知上下文
- 后端服务：`http://127.0.0.1:8001`
- 前端服务需要一起参与复测
- 当前目标是用真实运行服务验证整条任务流，不是只跑单元测试

## 当前状态
- 已创建问题记录
- 已完成一轮真实全链路复测

## 本次复测任务
- 任务 ID：`task_aec54bafe2`
- 使用模型：`gpt-5.4`
- 结果章节数：`3`

## 复测过程
- 大纲审核
  - 第一次：打回
  - 第二次：通过
- 正文审核
  - 第 1 组章节（第 1-2 章）：第一次打回，第二次通过
  - 第 2 组章节（第 3 章）：第一次打回，第二次通过
- 全文验证
  - 第一次：打回
  - 第二次：通过

## 前后端结果
- 后端 API 全链路已走通，任务最终完成并归档
- 前端页面已确认可访问：
  - `/tasks/task_aec54bafe2`
  - `/review/task_aec54bafe2`
  - `/result/task_aec54bafe2`

## 关键结果
- 本轮复测中，大纲与正文审核链路均按“先打回一次，再通过一次”完成
- 全文验证审核链路也按“先打回一次，再通过一次”完成，任务最终 `completed`
- 审核历史已正确保留 `waiting / submitted / rejected / approved`

## 本轮注意点
- 本次任务的全文验证报告为 `100 分 / 0 问题`
- 因此虽然验证打回链路被实际触发，但没有形成“基于问题清单的实质修复”样本
- 事件流中未出现 `fix-issues` 产物，这与“验证报告本身没有问题项”有关，本轮只能证明：
  - 验证审核的打回/再提交流程可达
  - 任务不会因打回而卡死

## 当前结论
- 当前代码下，前后端整体主链路已可跑通
- 大纲审核、章节审核、全文验证的“先打回后通过”流程都可以完成
- 如果需要继续验证“验证报告存在真实问题时，`fix_verified_issues` 是否产生修复产物并回到再审页”，还需要再构造一条确实会产出问题清单的任务样本

## 补充复测（按用户要求切换模型）
- 用户明确要求不再使用 `gpt-5.4`，改为 `glm-5.1`
- 新复测任务：
  - 任务 ID：`task_6b53157b3e`
  - 模型：`glm-5.1`
  - 最终章节数：`3`

## glm-5.1 复测结果
- 大纲审核
  - 第一次：打回
  - 第二次：通过
- 正文审核
  - 第 1 组章节（第 1-2 章）：第一次打回，第二次通过
  - 第 2 组章节（第 3 章）：第一次打回，第二次通过
- 全文验证
  - 第一次：打回
  - 第二次：通过
- 前端页面访问状态：
  - `/tasks/task_6b53157b3e` -> `200`
  - `/review/task_6b53157b3e` -> `200`
  - `/result/task_6b53157b3e` -> `200`

## glm-5.1 结论
- 使用 `glm-5.1` 时，前后端整条主链路同样已跑通
- 审核历史完整保留了 `waiting / submitted / rejected / approved`
- 本轮全文验证同样为 `100 分 / 0 问题`，因此仍未覆盖“存在真实问题清单时的 fix-issues 实修产物”样本

## 新一轮真实复测（切换 `api.udcode.cn/v1` 后）
- 探测任务：`task_8e1d68dd00`
- 模型：`glm-5.1`
- 初次大纲生成：成功进入 `waiting_outline_review`
- 但在“大纲先打回一次”后，任务没有继续走通，失败于大纲修订阶段

### 失败表现
- 任务状态：`failed`
- 错误摘要：`恢复执行失败：模型返回的 JSON 无法解析`
- 说明：
  - `glm-5.1` 在初次大纲生成时可以返回可解析 JSON
  - 但在 `outline_revision` 场景下返回内容被截断/不完整，导致 `StoryPlan.model_validate(...)` 失败

### 当前结论补充
- 当前 `api.udcode.cn/v1 + glm-5.1` 组合已经证明：
  - 网关接入已正确切换
  - 初次模型请求可成功
- 但完整链路还存在新的结构化输出稳定性问题：
  - **大纲修订阶段的模型输出不稳定，导致 JSON 解析失败**
