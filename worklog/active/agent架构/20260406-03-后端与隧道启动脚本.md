---
name: 后端与隧道启动脚本
description: 新增脚本一键启动后端服务与 cloudflared 内网穿透
type: project
---

# 后端与隧道启动脚本

## 用户原始诉求
- 编写脚本启动内网穿透和后端

## 当前已知上下文
- 后端端口使用 `8000`
- 前端与后端分离
- `cloudflared` 已安装，默认配置文件为 `~/.cloudflared/config.yml`
- 当前 tunnel 配置中 `api.yuegui666.icu` 指向 `localhost:8000`

## 当前状态
- 已创建问题记录
- 正在编写单独脚本启动后端与 cloudflared tunnel

## 本轮实现进展
- 已新增根目录脚本：
  - `start-backend-tunnel.sh`
- 脚本能力：
  - 启动后端 `uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload`
  - 读取 `~/.cloudflared/config.yml` 中的 `tunnel` ID
  - 执行 `cloudflared tunnel --config ... run <tunnel-id>`
  - 等待后端健康检查成功后再启动 tunnel
  - 支持 `--dry-run`
  - 支持 Ctrl+C 统一清理
- 已补充 `README.md` 使用说明

## 本轮验证记录
- 已通过脚本语法检查：
  - `bash -n start-backend-tunnel.sh`
- 已通过 dry-run 自检：
  - `./start-backend-tunnel.sh --dry-run`

## 当前状态
- 已完成脚本编写与最小自检
- 当前未执行 git 提交
