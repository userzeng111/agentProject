# MVP 审查后续日志覆盖与死代码清理

## 问题标题

MVP 审查后续日志覆盖与死代码清理

## 用户原始诉求

用户要求继续完成当前项目活跃任务并进行代码 review、设计实现、自动化测试、归档提交；审查过程中遗留“日志覆盖缺口与死代码清理”后续事项。

## 当前状态

第十批 `_graph_state_values()` 静默异常 warning 覆盖已完成：工作流 checkpoint 状态读取失败时会记录 warning，并继续保持 `{}` 降级语义；历史运行时死代码与日志覆盖清单仍有剩余，当前问题继续保持 active，不归档。

## 后续范围

- 复核历史 MVP 审查中识别的运行时死代码清理清单。
- 补齐仍缺失的关键异常日志覆盖。
- 每批清理需保持外部行为不变，并补充对应自动化测试。

## 2026-06-29 并行审计结论

- 历史 MVP 归档文档记录运行时死代码待清理：后端 62 项、前端 23 项，合计 85 项。
- 历史文档只保留分类数量，没有保留逐项权威清单，因此后续每批需要基于当前代码重新确认。
- 当前低风险清理点：
  - `apps/web/src/lib/api.ts` 中 `getModels`、`getSupervisor`、`fetchJsonRef` 只发现定义位置，未发现当前调用方。
  - `apps/agent-runtime/app/llm/model_catalog.py` 读取运行时默认模型失败时直接吞异常。
  - `apps/agent-runtime/app/llm/model_catalog.py` 调用网关 `list_models()` 失败时直接返回空列表。
- 中风险清理点如 `AgentRegistry`、`TaskLogStore` 部分方法仍可能被调试入口或共享存储边界间接依赖，不纳入第一批。

## 推荐实施切片：低风险第一批

- 删除前端 `api.ts` 中无调用的 `getModels`、`getSupervisor`、`fetchJsonRef` 导出。
- 给 `ModelCatalogService` 两处静默降级补 warning 日志，保持外部返回语义不变。
- 不改 `AgentRegistry`、`TaskLogStore`、`story_engine.py` 等中高风险区域。

## 推荐测试优先级

- 先补 `apps/web/src/lib/api.test.mjs`，验证模块不再暴露上述 3 个历史无调用导出。
- 先补 `apps/agent-runtime/tests/test_model_catalog.py`，覆盖运行时 settings 文件损坏不阻断启动但记录 warning。
- 先补 `apps/agent-runtime/tests/test_model_catalog.py`，覆盖网关模型列表异常时仍返回 fallback 模型目录并记录 warning。

## 2026-06-29 低风险第一批实施结果

- 删除 `apps/web/src/lib/api.ts` 中未发现调用方的历史导出：`getModels`、`getSupervisor`、`fetchJsonRef`。
- `ModelCatalogService` 读取运行时模型设置失败时记录 warning，并保持使用配置默认模型的降级语义。
- `ModelCatalogService` 读取网关模型列表失败时记录 warning，并保持返回本地模型画像降级目录的语义。
- 中风险清理点如 `AgentRegistry`、`TaskLogStore`、`story_engine.py` 未在本批次修改。

## 2026-06-29 验证结果

- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q`：342 passed，1 skipped，2 个既有 FastAPI `on_event` 弃用警告。
- `uv run --project apps/agent-runtime ruff check apps/agent-runtime/`：通过。
- `npm --prefix apps/web test`：91 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e`：96 passed。
- `python3 .agents/skills/project-interface-smoke/scripts/run_smoke.py --frontend-url http://localhost:3000 --backend-url http://localhost:8000`：全部通过。
- `python3 .agents/skills/api-full-test/scripts/run_full_test.py --backend-url http://localhost:8000 --frontend-url http://localhost:3000 --allow-gateway-unavailable`：17 通过，4 个本地/预期警告，0 失败。

## 后续待办

- 继续基于当前代码重新确认运行时死代码剩余项，分低风险批次清理。
- 继续补齐关键异常路径 warning/error 日志覆盖。
- 每批保持外部行为不变，并补对应自动化测试。

## 2026-06-30 第二批实施计划

- 已确认本轮第二批限定为低风险清理与日志覆盖：删除未使用字体 helper、删除未调用 `save_runtime_settings()`、补 `MODEL_PROTOCOL_OVERRIDES` 解析失败 warning、补模型兼容性缓存损坏 warning。
- 设计文档：`docs/superpowers/specs/2026-06-30-home-project-library-and-mvp-cleanup-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-home-project-library-and-mvp-cleanup.md`。
- 当前状态：第二批低风险清理与日志覆盖已完成。

## 2026-06-30 第二批实施结果

- 删除前端未使用的 `apps/web/src/lib/fonts.ts`，保留 `globals.css` 中实际使用的中文字体 CSS 变量。
- 新增字体 fallback 轻量测试，防止后续误删 `--font-sans-sc` 与 `--font-serif-sc`。
- 删除后端未调用公共函数 `save_runtime_settings()`，保留内部原子写入辅助函数。
- 扩展运行时设置测试，覆盖模型协议覆盖项的写入与删除 round-trip。
- `MODEL_PROTOCOL_OVERRIDES` 环境变量 JSON 解析失败时记录 warning，并保持降级为空映射。
- 模型兼容性缓存 JSON 损坏时记录 warning，并保持返回 `unverified` 的降级语义。

## 2026-06-30 验证结果

- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_runtime_settings.py apps/agent-runtime/tests/test_settings_and_gateway_fail_fast.py apps/agent-runtime/tests/test_model_compatibility.py -q`：35 passed，4 subtests passed。
- `uv run --project apps/agent-runtime ruff check apps/agent-runtime/`：通过。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q`：362 passed，1 skipped，2 warnings，4 subtests passed。
- `npm --prefix apps/web test`：105 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e`：98 passed。

## 后续待办更新

- 继续基于当前代码重新确认运行时死代码剩余项，分低风险批次清理。
- 继续补齐关键异常路径 warning/error 日志覆盖。
- 每批保持外部行为不变，并补对应自动化测试。

## 2026-06-30 第三批实施计划

- 已完成并行只读审计：优先处理模型兼容性与网关协议相关的低风险日志覆盖，不改返回语义。
- 本批后端范围：
  - 修复损坏 `model_compatibility.json` 在模型目录多模型聚合时重复 warning 的日志放大问题。
  - `_invalidate_model_catalog_cache()`、`_gateway_visible()`、`OpenAICompatibleGatewayClient._resolve_protocol()` 异常降级时补 warning。
- 暂不处理 `TaskLogStore`、动态 Agent 栈、`StoryEngine` 主链路等中高风险区域；前端 test-only 组件删除留到后续独立批次。
- 设计文档：`docs/superpowers/specs/2026-06-30-project-shell-stage-nav-and-mvp-third-cleanup-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-project-shell-stage-nav-and-mvp-third-cleanup.md`。
- 当前状态：本批已按 TDD 完成实现与验证。

## 2026-06-30 第三批实施结果

- `ModelCompatibilityService` 增加实例内 store 缓存与文件指纹，避免重复读取和模型目录聚合时的损坏文件 warning 放大。
- 损坏 `model_compatibility.json` 同一文件指纹只记录一次 warning；文件外部修复或变化后能按新指纹重新读取。
- `save_report()` 与 `clear_report()` 后缓存状态同步更新，避免继续读到旧报告。
- `_invalidate_model_catalog_cache()` 异常降级补 warning，保持保存验证报告不抛出。
- `_gateway_visible()` 模型目录 resolver 异常补 warning，保持验证流以 `gateway_visible` 失败收敛。
- `OpenAICompatibleGatewayClient._resolve_protocol()` 动态协议覆盖 resolver 异常补 warning，保持默认协议降级语义。
- 本批没有触碰 `TaskLogStore`、动态 Agent 栈、`StoryEngine` 主链路等中高风险区域。

## 2026-06-30 第三批验证结果

- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q`：369 passed，1 skipped，2 warnings，4 subtests passed。
- `uv run --project apps/agent-runtime ruff check apps/agent-runtime/`：通过。
- 前端联动门禁同步通过：`npm --prefix apps/web test` 为 107 passed；`npm --prefix apps/web run lint` 通过；`npm --prefix apps/web run build` 通过，仅保留既有 `metadataBase` 警告；`npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts home-create.spec.ts --project=chromium` 为 12 passed；`npm --prefix apps/web run test:e2e` 为 98 passed。

## 2026-06-30 后续待办更新

- 继续基于当前代码重新确认运行时死代码剩余项，分低风险批次清理。
- 继续补齐关键异常路径 warning/error 日志覆盖。
- 前端 test-only 组件删除与 `TaskLogStore`、动态 Agent 栈、`StoryEngine` 相关清理需后续独立评估后再做。

## 2026-06-30 第七批实施计划

- 已完成多 agent 只读审计：`OpenAICompatibleGatewayClient._provider_prompt_cache_kwargs()` 在读取全局设置失败时直接返回 `{}`，缺少 warning，属于低风险日志覆盖缺口。
- 本批后端范围限定为 `apps/agent-runtime/app/llm/gateway_client.py`，只在 `get_settings()` 异常分支记录 `读取 provider prompt cache 设置失败` warning。
- 保持非 Anthropic adapter、设置读取成功路径、prompt cache 参数格式和 `{}` 降级语义不变。
- 新增 `test_settings_and_gateway_fail_fast.py` 覆盖设置读取失败时返回 `{}` 且记录 warning。
- 暂不处理前端 test-only 组件删除、`TaskLogStore`、legacy 模型验证流式路由、动态 Agent 栈和 `StoryEngine` 主链路。
- 设计文档：`docs/superpowers/specs/2026-06-30-workspace-shell-and-gateway-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-workspace-shell-and-gateway-warning.md`。
- 当前状态：本批已按 TDD 完成实现与验证；本 active 仍不归档。

## 2026-06-30 第七批实施结果

- `OpenAICompatibleGatewayClient._provider_prompt_cache_kwargs()` 在 `get_settings()` 抛异常时记录 `读取 provider prompt cache 设置失败` warning。
- 异常分支继续返回 `{}`，保持 provider prompt cache 降级禁用语义。
- 非 Anthropic adapter、设置读取成功路径、prompt cache 参数格式、协议选择逻辑均未修改。
- 新增 `test_prompt_cache_settings_failure_logs_warning_and_disables_prompt_cache` 覆盖 warning 与 `{}` 返回。
- 本批未触碰前端 test-only 组件、`TaskLogStore`、legacy 模型验证流式路由、动态 Agent 栈和 `StoryEngine` 主链路。

## 2026-06-30 第七批验证结果

- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_settings_and_gateway_fail_fast.py::SettingsAndGatewayFailFastTests::test_prompt_cache_settings_failure_logs_warning_and_disables_prompt_cache -q`：1 passed，红灯阶段曾因无 warning 失败。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_settings_and_gateway_fail_fast.py -q`：24 passed。
- `uv run --project apps/agent-runtime ruff check apps/agent-runtime/`：通过。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q`：379 passed，1 skipped，2 warnings，4 subtests passed。
- 前端联动门禁同步通过：`npm --prefix apps/web test` 为 112 passed；`npm --prefix apps/web run lint` 通过；`npm --prefix apps/web run build` 通过，仅保留既有 `metadataBase` 警告；`npm --prefix apps/web run test:e2e` 为 104 passed，最终轮次覆盖长任务 ID 移动端工作台用例。

## 2026-06-30 后续待办更新

- 继续基于当前代码重新确认运行时死代码剩余项，分低风险批次清理。
- 继续补齐关键异常路径 warning/error 日志覆盖。
- 前端 test-only 组件删除与 `TaskLogStore`、legacy 模型验证流式路由、动态 Agent 栈、`StoryEngine` 主链路清理需后续独立评估后再做。

## 2026-06-30 第八批实施计划

- 已完成并行只读审计：下一批后端首选任务 SSE 终态检查异常 warning，范围小且属于前端实际使用的工作台事件流。
- 本批只处理 `stream_task_events()` 内 `task_service.store.get(task_id)` 异常静默 `pass`，增加一次性 warning，避免 15 秒循环刷日志。
- 异常时继续等待 queue 或 keep-alive，保持 `snapshot`、`task.event`、`task.done` SSE 语义不变。
- 暂不处理 `_graph_state_values()`、前端 test-only 组件、`TaskLogStore`、legacy 模型验证流式路由、动态 Agent 栈和 `StoryEngine` 主链路。
- 设计文档：`docs/superpowers/specs/2026-06-30-chapter-progress-panel-and-sse-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-chapter-progress-panel-and-sse-warning.md`。
- 当前状态：本批已按 TDD 完成实现与验证；本 active 仍不归档。

## 2026-06-30 第八批实施结果

- `stream_task_events()` 的 `event_stream()` 增加 `terminal_check_warning_logged` 标记。
- `task_service.store.get(task_id)` 终态检查抛异常时，首次记录 `任务事件流终态检查失败 task_id=%s` warning，并保留 `exc_info=True`。
- 异常后继续进入原有 queue wait / keep-alive 流程，保持 `snapshot`、`task.event`、`task.done` SSE 输出语义不变。
- 新增 API 集成测试覆盖 `store.get()` 连续两轮抛异常时仍能输出 snapshot、keep-alive 注释、任务事件和 done，并断言 warning 只记录一次。
- 本批未触碰 `_graph_state_values()`、前端 test-only 组件、`TaskLogStore`、legacy 模型验证流式路由、动态 Agent 栈和 `StoryEngine` 主链路。

## 2026-06-30 第八批验证结果

- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_api_context.py::ApiContextIntegrationTests::test_task_event_stream_warns_once_when_terminal_status_check_fails -q`：1 passed，红灯阶段曾因无 `app.api.routes` warning 失败；审查反馈后加强为连续两轮终态检查失败与 keep-alive 路径覆盖。
- `uv run --project apps/agent-runtime ruff check apps/agent-runtime/`：通过。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q`：380 passed，1 skipped，2 warnings，4 subtests passed。
- 前端联动门禁同步通过：`npm --prefix apps/web test` 为 112 passed；`npm --prefix apps/web run lint` 通过；`npm --prefix apps/web run build` 通过，仅保留既有 `metadataBase` 警告；`npm --prefix apps/web run test:e2e -- task-state-branches.spec.ts --project=chromium` 为 16 passed；`npm --prefix apps/web run test:e2e` 为 106 passed。

## 2026-06-30 后续待办更新

- 继续基于当前代码重新确认运行时死代码剩余项，分低风险批次清理。
- 继续补齐关键异常路径 warning/error 日志覆盖。
- 前端 test-only 组件删除与 `TaskLogStore`、legacy 模型验证流式路由、动态 Agent 栈、`StoryEngine` 主链路清理需后续独立评估后再做。

## 2026-06-30 第九批实施计划

- 已完成多 agent 只读审计：`LoadingOverlay`、`PageContainer`、`ModelSelect`、`ConfirmDialog`、`DecisionBar`、`StageBadge`、`ProgressBar`、`SkeletonGrid`、`EmptyState` 仅命中自身定义和 import-only smoke 测试，未发现生产调用方。
- 本批只删除上述 test-only 组件和对应 import-only 测试，不改任何生产调用点。
- 删除前后使用 `rg` 验证无生产引用和无残留引用，并通过前端测试、lint、build 与 E2E 兜底。
- 暂不处理 `TaskLogStore`、legacy 模型验证流式路由、动态 Agent 栈、`StoryEngine` 主链路和其它运行时死代码。
- 设计文档：`docs/superpowers/specs/2026-06-30-workflow-graph-dark-and-test-only-cleanup-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-workflow-graph-dark-and-test-only-cleanup.md`。
- 当前状态：本批已完成删除、审查与验证；本 active 仍不归档。

## 2026-06-30 第九批实施结果

- 删除 `LoadingOverlay`、`PageContainer`、`ModelSelect`、`ConfirmDialog`、`DecisionBar`、`StageBadge`、`ProgressBar`、`SkeletonGrid`、`EmptyState` 及对应 import-only smoke 测试。
- 删除前 `rg` 确认目标组件未发现生产调用方，删除后精确单词边界扫描无残留目标组件引用。
- `ReviewActionModelSelector` 仅为名称子串命中，不属于本批删除对象，已保留。
- 本批未触碰 `TaskLogStore`、legacy 模型验证流式路由、动态 Agent 栈和 `StoryEngine` 主链路。

## 2026-06-30 第九批验证结果

- 删除前引用扫描：目标组件只命中自身定义；`ReviewActionModelSelector` 为非目标子串命中。
- 删除后精确残留扫描：`rg -n "\\b(LoadingOverlay|PageContainer|ModelSelect|ConfirmDialog|DecisionBar|StageBadge|ProgressBar|SkeletonGrid|EmptyState)\\b" apps/web/src` 无输出。
- `npm --prefix apps/web test`：112 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e`：108 passed。
- 后端联动门禁同步通过：`uv run --project apps/agent-runtime ruff check apps/agent-runtime/` 通过；`uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q` 为 380 passed，1 skipped，2 warnings，4 subtests passed。

## 2026-06-30 第十批实施计划

- 已完成多 agent 只读审计：`TaskService._graph_state_values()` 对 `workflow_engine.get_state()` 异常直接返回 `{}`，缺少 warning，调用点包括 checkpoint 同步、resume 和 recovery 回填。
- 本批只在异常分支记录 `读取工作流 checkpoint 状态失败 task_id=%s` warning，并保留 `exc_info=True`。
- 返回值继续为 `{}`，不改变 `_sync_checkpoint_with_db()`、恢复回填、resume 和 recovery 的降级语义。
- 新增服务层测试覆盖 fake workflow engine 抛异常时返回 `{}` 且记录 warning。
- 暂不处理 `TaskLogStore`、legacy `/api/model-validation/stream` 删除、动态 Agent 栈和 `StoryEngine` 主链路。
- 设计文档：`docs/superpowers/specs/2026-06-30-markdown-dark-code-and-graph-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-markdown-dark-code-and-graph-warning.md`。
- 当前状态：本批已按 TDD 完成实现、审查与验证；本 active 仍不归档。

## 2026-06-30 第十批实施结果

- `TaskService._graph_state_values()` 在 `workflow_engine.get_state()` 抛异常时记录 `读取工作流 checkpoint 状态失败 task_id=%s` warning，并保留 `exc_info=True`。
- 异常分支继续返回 `{}`，不改变 `_sync_checkpoint_with_db()`、恢复回填、resume 和 recovery 的降级语义。
- 新增 `test_graph_state_values_warns_when_get_state_fails`，覆盖 fake workflow engine 抛异常时返回 `{}` 且 warning 包含中文消息和 task id。
- 本批未触碰 `TaskLogStore`、legacy `/api/model-validation/stream` 删除、动态 Agent 栈和 `StoryEngine` 主链路。
- 只读审查未发现阻塞问题；与本批相关的 warning 返回语义保持不变。

## 2026-06-30 第十批验证结果

- 红灯：`uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_task_service_review_resume.py::TaskServiceReviewResumeTests::test_graph_state_values_warns_when_get_state_fails -q` 曾因异常分支无 warning 失败。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_task_service_review_resume.py::TaskServiceReviewResumeTests::test_graph_state_values_warns_when_get_state_fails -q`：1 passed。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_task_service_review_resume.py -q`：33 passed。
- `uv run --project apps/agent-runtime ruff check apps/agent-runtime/`：通过。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q`：381 passed，1 skipped，2 warnings，4 subtests passed。
- 前端联动门禁同步通过：`npm --prefix apps/web test` 为 114 passed；`npm --prefix apps/web run lint` 通过；`npm --prefix apps/web run build` 通过，仅保留既有 `metadataBase` 警告；`npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts --project=chromium` 为 9 passed；`npm --prefix apps/web run test:e2e` 为 108 passed。

## 2026-06-30 后续待办更新

- 继续基于当前代码重新确认运行时死代码剩余项，分低风险批次清理。
- 继续补齐关键异常路径 warning/error 日志覆盖。
- `TaskLogStore`、legacy `/api/model-validation/stream`、动态 Agent 栈和 `StoryEngine` 主链路清理需后续独立评估后再做。

## 2026-06-30 第六批实施计划

- 已完成多 agent 只读审计：本批优先处理 RAG workspace 状态异常路径 warning 覆盖，并清理前端未使用的主题初始化 helper。
- 后端范围限定为 `TaskServiceQueriesMixin._build_rag_status()`：RAG `is_ready()` 或 `readiness_error()` 抛异常时记录 warning，保持 `rag_status` 返回结构和降级语义不变。
- 前端低风险清理限定为 `getInitialMode()`：当前仅测试文件调用，运行时 `ThemeModeProvider` 已直接在 `useEffect` 中读取 `STORAGE_KEY`。
- 暂不处理 `TaskLogStore` supervisor helper、legacy 模型验证流式路由、动态 Agent 栈和 `StoryEngine` 主链路。
- 设计文档：`docs/superpowers/specs/2026-06-30-review-split-and-rag-workspace-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-review-split-and-rag-workspace-warning.md`。
- 当前状态：本批已按 TDD 完成实现与验证；本 active 仍不归档。

## 2026-06-30 第五批实施计划

- 已完成并行只读审计：下一批优先做上下文缓存与磁盘缓存 warning 行为测试，不动业务逻辑；前端 test-only 组件删除继续留到独立批次。
- 本批后端范围：
  - `ContextManager.build_snapshot()` 读缓存失败、写缓存失败 warning 断言。
  - `FileBackedCacheStore.get()` 损坏 JSON warning 断言。
  - `LayeredCacheStore.get()` / `set()` 单层异常 warning 断言。
- 同批因 UI 归档列表真实指标需要，允许 `TaskSummary` 增加向后兼容的 `chapter_count` / `word_count` 可选字段，并补 `/api/archive` contract 测试。
- 暂不处理 `TaskLogStore`、动态 Agent 栈、`StoryEngine` 主链路和包级 re-export 清理。
- 设计文档：`docs/superpowers/specs/2026-06-30-archive-list-and-context-cache-logs-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-archive-list-and-context-cache-logs.md`。
- 当前状态：本批已按 TDD 完成实现与验证；本 active 仍不归档。

## 2026-06-30 第四批实施计划

- 已完成并行只读审计：本批只处理 RAG status 文件损坏时静默降级缺少 warning 的低风险日志覆盖。
- 后端范围限定为 `NovelCorpusRebuildService._load_status()`，保持 `get_status()` 对外返回结构和文件不存在时的静默行为不变。
- 新增损坏 JSON 与 JSON 非对象两类测试，确保异常路径有 warning 且 `last_result` 继续降级为 `None`。
- 暂不处理前端 test-only 组件删除、`TaskLogStore`、动态 Agent 栈、`StoryEngine` 主链路等中高风险区域。
- 设计文档：`docs/superpowers/specs/2026-06-30-archive-reader-shell-and-rag-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-archive-reader-shell-and-rag-warning.md`。
- 当前状态：本批已按 TDD 完成实现与验证。

## 2026-06-30 第四批实施结果

- RAG status 文件不存在时继续返回 `None`，不记录 warning，保留原静默语义。
- RAG status 文件存在但读取或 JSON 解析失败时记录 `读取 RAG 重建状态失败` warning，并返回 `None`。
- RAG status 文件可解析但顶层不是对象时记录 `RAG 重建状态格式不正确` warning，并返回 `None`。
- 新增测试覆盖损坏 JSON 与 JSON 数组两种异常路径。

## 2026-06-30 第四批验证结果

- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_rag_rebuild.py -q`：5 passed。
- `uv run --project apps/agent-runtime ruff check apps/agent-runtime/`：通过。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q`：371 passed，1 skipped，2 warnings，4 subtests passed。
- 前端联动门禁同步通过：`npm --prefix apps/web test` 为 111 passed；`npm --prefix apps/web run lint` 通过；`npm --prefix apps/web run build` 通过，仅保留既有 `metadataBase` 警告；`npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts --project=chromium` 为 7 passed；`npm --prefix apps/web run test:e2e` 为 98 passed。

## 2026-06-30 后续待办更新

- 继续基于当前代码重新确认运行时死代码剩余项，分低风险批次清理。
- 继续补齐关键异常路径 warning/error 日志覆盖。
- 前端 test-only 组件删除与 `TaskLogStore`、动态 Agent 栈、`StoryEngine` 相关清理需后续独立评估后再做。

## 2026-06-30 第六批实施结果

- `TaskServiceQueriesMixin._build_rag_status()` 在 `rag_service.is_ready()` 异常时记录 `读取 RAG 工作区就绪状态失败` warning，并保持 `rag_status.ready=false` 与 `last_error` 降级语义。
- `TaskServiceQueriesMixin._build_rag_status()` 在 `rag_service.readiness_error()` 异常时记录 `读取 RAG 工作区未就绪原因失败` warning，并保持 `last_error` 返回异常原因。
- 新增两个 workspace characterization tests，分别覆盖 `is_ready()` 与 `readiness_error()` 异常路径。
- 删除前端未使用的 `getInitialMode()` 导出，`ThemeModeProvider` 仍沿用现有 `STORAGE_KEY` 读取流程。
- 本批未触碰 `TaskLogStore` supervisor helper、legacy 模型验证流式路由、动态 Agent 栈和 `StoryEngine` 主链路。

## 2026-06-30 第六批验证结果

- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_task_service_workspace.py::TaskServiceWorkspaceTests::test_workspace_rag_status_warns_when_readiness_check_fails apps/agent-runtime/tests/test_task_service_workspace.py::TaskServiceWorkspaceTests::test_workspace_rag_status_warns_when_readiness_error_fails -q`：2 passed。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_task_service_workspace.py -q`：24 passed。
- `uv run --project apps/agent-runtime ruff check apps/agent-runtime/`：通过。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q`：378 passed，1 skipped，2 warnings，4 subtests passed。
- 前端联动门禁同步通过：`npm --prefix apps/web test` 为 111 passed；`npm --prefix apps/web run lint` 通过；`npm --prefix apps/web run build` 通过，仅保留既有 `metadataBase` 警告；`npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts` 为 18 passed；`npm --prefix apps/web run test:e2e` 为 102 passed。

## 2026-06-30 后续待办更新

- 继续基于当前代码重新确认运行时死代码剩余项，分低风险批次清理。
- 继续补齐关键异常路径 warning/error 日志覆盖。
- 前端 test-only 组件删除与 `TaskLogStore`、legacy 模型验证流式路由、动态 Agent 栈、`StoryEngine` 主链路清理需后续独立评估后再做。

## 2026-06-30 第五批实施结果

- `ContextManager.build_snapshot()` 读缓存失败、写缓存失败的 warning 行为已补 characterization tests，异常时仍返回正常 snapshot。
- `FileBackedCacheStore.get()` 损坏 JSON 的 warning 行为已补测试，继续降级返回 `None`。
- `LayeredCacheStore.get()` 单层读取异常继续读后续层，`set()` 单层写入异常继续写后续层，两条 warning 行为均已补测试。
- 归档 summary API contract 同步补充 `/api/archive` 章节数与字数指标断言，防止前端作品库卡片只依赖 fixture。
- 本批未触碰 `TaskLogStore`、动态 Agent 栈、`StoryEngine` 主链路和包级 re-export 清理。

## 2026-06-30 第五批验证结果

- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_api_context.py::ApiContextIntegrationTests::test_result_and_archive_endpoints_expose_json_refs_and_sources apps/agent-runtime/tests/test_context_manager.py -q`：11 passed。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_context_manager.py -q`：10 passed。
- `uv run --project apps/agent-runtime ruff check apps/agent-runtime/`：通过。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q`：376 passed，1 skipped，2 warnings，4 subtests passed。
- 前端联动门禁同步通过：`npm --prefix apps/web test` 为 111 passed；`npm --prefix apps/web run lint` 通过；`npm --prefix apps/web run build` 通过，仅保留既有 `metadataBase` 警告；`npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts --project=chromium` 为 8 passed；`npm --prefix apps/web run test:e2e` 为 100 passed。

## 2026-06-30 后续待办更新

- 继续基于当前代码重新确认运行时死代码剩余项，分低风险批次清理。
- 继续补齐关键异常路径 warning/error 日志覆盖。
- 前端 test-only 组件删除与 `TaskLogStore`、动态 Agent 栈、`StoryEngine` 相关清理需后续独立评估后再做。
