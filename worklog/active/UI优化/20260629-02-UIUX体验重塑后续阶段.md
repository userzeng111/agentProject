# UI/UX 体验重塑后续阶段

## 问题标题

UI/UX 体验重塑后续阶段

## 用户原始诉求

用户要求继续完成当前项目活跃任务；UI/UX 体验重塑完整目标尚未完成，阶段 1 已收口归档，后续阶段需继续推进。

## 当前状态

本轮聊天页移动端可访问性与长文本溢出兜底切片已完成：思考过程按钮语义、Enter/Space 键盘操作、消息断词、长模型名称/Provider 显示、聊天标题栏与顶部导航 320px 横向溢出兜底均已补齐；阶段 4 全页面精调仍未完成，当前问题继续保持 active，不归档。

## 后续范围

- 阶段 2：首页与作品库视角、`/new` 创建流程、`/p/[projectId]` 路由迁移。
- 阶段 3：ProjectShell、StageNav、审核页左右分屏、结果阅读器、章节与归档体验重构。
- 阶段 4：暗黑模式全页面精调、移动端布局、可访问性与大组件拆分。

## 风险与约束

- 不得把阶段 1 完成误写为完整 UI/UX 重塑完成。
- 后续实施前需重新确认设计范围，分阶段补自动化测试。

## 2026-06-29 并行审计结论

- 当前前端已具备阶段 1 基础：主题、深浅色基础、通用组件层与 E2E 抽象文件已存在。
- 阶段 2 关键对象仍未落地：未发现 `ProjectShell`、`StageNav`、`ProjectCard`、`/new`、`/p/[projectId]` 等实现。
- 当前页面仍使用旧路由：`/create`、`/tasks?id=`、`/review?id=`、`/result?id=`、`/archive/detail?id=`。
- 最高风险：`apps/web/next.config.mjs` 当前启用 `output: "export"`，动态 `/p/[projectId]` 与真实 302 路由重定向可能需要额外处理，不能直接假设 Next 静态导出可覆盖。

## 推荐实施切片：阶段 2A

- 新增 `/new`，先复用现有创建页客户端。
- 新增 `/p/[projectId]`，先复用现有 `TaskRunClient` 并通过 route param 传入 `taskId`。
- 更新 `task-routes.ts` 与 E2E 路由抽象，让新入口生成 `/new` 与 `/p/{projectId}`。
- 旧 `/create` 与 `/tasks?id=...` 先保持兼容跳转。
- 暂不纳入完整三步向导、审核页左右分屏、结果阅读器、移动端底部 Tab 与大组件拆分。

## 推荐测试优先级

- 先补 `apps/web/src/lib/task-routes.test.mjs`，直接导入真实 helper，验证新 canonical 路由。
- 新增/调整 E2E，覆盖 `/new` 可达、创建成功进入 `/p/{id}`、旧入口兼容跳转。
- 实现后至少运行相关前端单测、路由迁移 E2E、lint 与 build；若触发静态导出限制，先在测试中暴露并收敛处理方案。

## 2026-06-29 阶段 2A 实施结果

- 新增 `/new` 创建入口，复用现有创建任务客户端。
- 新增 `/p/[projectId]` 项目工作台入口，复用现有任务工作台客户端。
- 旧 `/create` 兼容跳转到 `/new`，并保留查询参数。
- 旧 `/tasks?id=...` 兼容跳转到 `/p/{id}`；无 id 时继续保留旧工作台兜底。
- `workspaceHref()` 改为生成 `/p/{id}`，新增 `newProjectHref()`。
- 首页主按钮、顶部导航、失败任务重新创建、相关 E2E helper 与测试入口已切到新路由。
- 修复审查发现的静态导出占位风险：`/p/[projectId]` 改为静态壳，客户端从浏览器 pathname 解析真实任务 id；FastAPI SPA fallback 对 `/p/{id}` 返回项目页静态壳，避免 `__placeholder__` 泄漏到业务请求。

## 2026-06-29 验证结果

- `npm --prefix apps/web test`：91 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e`：96 passed。
- 生产静态直达验证：`http://localhost:8010/p/task_prod_fixture` 请求真实 `task_prod_fixture` 工作台与事件流，未请求 `__placeholder__`。
- `python3 .agents/skills/project-interface-smoke/scripts/run_smoke.py --frontend-url http://localhost:3000 --backend-url http://localhost:8000`：全部通过。
- `python3 .agents/skills/api-full-test/scripts/run_full_test.py --backend-url http://localhost:8000 --frontend-url http://localhost:3000 --allow-gateway-unavailable`：17 通过，4 个本地/预期警告，0 失败。

## 后续待办

- 阶段 2B：作品库视角与项目卡片体验仍待设计实现。
- 阶段 3：ProjectShell、StageNav、审核页左右分屏、结果阅读器、章节与归档体验重构仍待推进。
- 阶段 4：暗黑模式全页面精调、移动端布局、可访问性与大组件拆分仍待推进。

## 2026-06-30 阶段 2B 实施计划

- 已确认本轮只做首页作品库视角与项目卡片体验，不迁移审核、结果、归档详情旧路由。
- 设计文档：`docs/superpowers/specs/2026-06-30-home-project-library-and-mvp-cleanup-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-home-project-library-and-mvp-cleanup.md`。
- 当前状态：已按 TDD 完成首页作品库 E2E 与最小 UI 实现。

## 2026-06-30 阶段 2B 实施结果

- 首页任务列表调整为“作品库”项目卡片视角，保留分组 Tab、分页、失败重试、删除与默认模型侧栏。
- 项目卡片新增稳定定位 `data-testid="project-card"`，主操作文案改为“进入项目”，并继续指向 `/p/{projectId}`。
- 作品摘要、任务状态、阶段、体裁来源等元信息改为更适合浏览的卡片信息层级。
- 修复移动端首页 MUI Grid spacing 导致的横向溢出风险。
- 聊天发送按钮补充 `aria-label="发送消息"`，E2E 改为按无障碍名称定位，降低选择器脆弱性。

## 2026-06-30 验证结果

- `npm --prefix apps/web test`：105 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e -- home-create.spec.ts --project=chromium`：5 passed。
- `npm --prefix apps/web run test:e2e -- chat-settings.spec.ts --project=chromium`：4 passed。
- `npm --prefix apps/web run test:e2e`：98 passed。
- `python3 .agents/skills/project-interface-smoke/scripts/run_smoke.py --frontend-url http://localhost:3000 --backend-url http://localhost:8000`：全部通过。
- `python3 .agents/skills/api-full-test/scripts/run_full_test.py --backend-url http://localhost:8000 --frontend-url http://localhost:3000 --allow-gateway-unavailable`：19 OK，2 WARN，0 FAIL；两个 WARN 为 review/result 任务未进入对应阶段的预期跳过。

## 后续待办更新

- 阶段 3：ProjectShell、StageNav、审核页左右分屏、结果阅读器、章节与归档体验重构仍待推进。
- 阶段 4：暗黑模式全页面精调、移动端布局、可访问性与大组件拆分仍待推进。

## 2026-06-30 阶段 3 最小切片计划

- 已完成并行只读审计：当前没有真正的 `ProjectShell`、`StageNav` 或等价项目级共享组件；审核页和结果页都有重复阶段条与项目元信息，且已有 E2E 覆盖，适合作为首批入口。
- 本批只做审核页与结果页的 `StageNav`、轻量 `ProjectShell` 接入，不接 `/p/[projectId]` 工作台，不改审核/结果路由，不抽归档阅读器。
- 同步修复上一批审查发现的首页项目卡片长任务 ID 移动端潜在横向溢出风险。
- 设计文档：`docs/superpowers/specs/2026-06-30-project-shell-stage-nav-and-mvp-third-cleanup-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-project-shell-stage-nav-and-mvp-third-cleanup.md`。
- 当前状态：本批已按 TDD 完成实现与验证。

## 2026-06-30 阶段 3 最小切片实施结果

- 新增统一 `StageNav`，提供 `resolveStageNavItems()` 轻量单测入口，阶段状态覆盖 done/current/upcoming，并保留横向滚动与当前态标识。
- 新增轻量 `ProjectShell`，统一审核页与结果页顶部面包屑、标题、元信息和阶段导航插槽。
- 审核页大纲、章节审核、验证审核分支接入 `ProjectShell` 与 `StageNav`，保留原有审核操作与路由语义。
- 结果页接入 `ProjectShell` 与 `StageNav`，复制全文和导出 MD 继续保留在正文卡片内。
- 首页项目卡片底部长任务 ID 增加断词约束，E2E fixture 改用长不可断 ID 覆盖移动端横向溢出风险。

## 2026-06-30 阶段 3 最小切片验证结果

- `npm --prefix apps/web test`：107 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts home-create.spec.ts --project=chromium`：12 passed。
- `npm --prefix apps/web run test:e2e`：98 passed。
- 后端联动门禁同步通过：`uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q` 为 369 passed，1 skipped，2 warnings，4 subtests passed；`uv run --project apps/agent-runtime ruff check apps/agent-runtime/` 通过。

## 2026-06-30 后续待办更新

- 阶段 3：审核页左右分屏、结果阅读器、章节与归档体验重构仍待推进。
- 阶段 4：暗黑模式全页面精调、移动端布局、可访问性与大组件拆分仍待推进。

## 2026-06-30 阶段 3 阅读器与归档详情切片计划

- 已完成并行只读审计：阶段 3 剩余高收益入口为结果阅读器、归档详情页面壳与归档阅读器暗色硬编码清理。
- 本批只做共享 `NovelReader`、结果页按章阅读体验、归档详情 `ProjectShell` 接入与 `StageNav` 无图标兜底回归修复。
- 不迁移 `/result?id=` 与 `/archive/detail?id=` 路由，不改 `/p/[projectId]` 工作台，不处理归档列表重构。
- 设计文档：`docs/superpowers/specs/2026-06-30-archive-reader-shell-and-rag-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-archive-reader-shell-and-rag-warning.md`。
- 当前状态：本批已按 TDD 完成实现与验证。

## 2026-06-30 阶段 3 阅读器与归档详情实施结果

- `StageNav` 在阶段未传 `icon` 时改为显示阶段序号，避免当前阶段圆点为空。
- 新增共享 `NovelReader`，统一章节翻页、页码 clamp、正文呈现与空章节提示，使用主题 token，移除归档阅读器旧硬编码浅色背景。
- 结果页在全文卡片前增加按章阅读器，保留章节索引、复制全文与导出 MD 工作流。
- 归档详情接入 `ProjectShell`，保留 Tab query 同步、复制全文、导出 MD、Snackbar 反馈，并用共享 `NovelReader` 替换局部阅读器。
- E2E 覆盖结果页阅读器、归档详情 `ProjectShell`、`tab=read` 阅读器、Tab query、复制与导出入口。

## 2026-06-30 阶段 3 阅读器与归档详情验证结果

- `npm --prefix apps/web test -- src/components/stage-nav.test.mjs`：3 passed。
- `npm --prefix apps/web test -- src/components/novel-reader.test.mjs src/components/stage-nav.test.mjs`：6 passed。
- `npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts --project=chromium`：7 passed。
- `npm --prefix apps/web test`：111 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e`：98 passed。
- 后端联动门禁同步通过：`uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q` 为 371 passed，1 skipped，2 warnings，4 subtests passed；`uv run --project apps/agent-runtime ruff check apps/agent-runtime/` 通过。

## 2026-06-30 后续待办更新

- 阶段 3：审核页左右分屏、归档列表体验、章节与归档体验进一步重构仍待推进。
- 阶段 4：暗黑模式全页面精调、移动端布局、可访问性与大组件拆分仍待推进。

## 2026-06-30 归档列表体验切片计划

- 已完成并行只读审计：归档列表仍是旧式单列卡片，文件范围小，适合作为阶段 3 下一批 UI 切片；审核页左右分屏、`TaskRunClient` 拆分和全站暗黑模式暂不混入本批。
- 本批目标：归档列表升级为作品库式卡片，展示章节数、字数、类型、模型、更新时间，并补移动端无横向滚动验证。
- 设计文档：`docs/superpowers/specs/2026-06-30-archive-list-and-context-cache-logs-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-archive-list-and-context-cache-logs.md`。
- 计划审查发现归档章节数/字数不能只靠前端 fixture，因此本批允许后端 `TaskSummary` 增加向后兼容的可选 `chapter_count` / `word_count` 字段，并补 API contract 测试。
- 当前状态：本批已按 TDD 完成实现与验证；本 active 仍不归档。

## 2026-06-30 归档列表体验切片实施结果

- 归档列表改为作品库式卡片，每个归档项增加稳定定位 `data-testid="archive-card"`。
- 卡片展示标题、摘要、章节数、字数、创作类型、默认模型、最近动作模型与更新时间，并保留详情入口。
- 标题、摘要和模型字段增加断词约束，补移动端 390px 无横向滚动 E2E 覆盖。
- 后端 `TaskSummary` 增加向后兼容的 `chapter_count` / `word_count` 可选字段，归档列表从 `draft_result.chapters` 计算真实章节数和近似字数；没有正文时章节数回退到 `story_plan.chapter_plan`。
- 归档列表 fixture 增加长标题、长模型 ID 和真实指标，用于覆盖作品库卡片展示与移动端溢出风险。

## 2026-06-30 归档列表体验切片验证结果

- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_api_context.py::ApiContextIntegrationTests::test_result_and_archive_endpoints_expose_json_refs_and_sources apps/agent-runtime/tests/test_context_manager.py -q`：11 passed。
- `npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts --project=chromium`：8 passed。
- `npm --prefix apps/web test`：111 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `uv run --project apps/agent-runtime ruff check apps/agent-runtime/`：通过。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q`：376 passed，1 skipped，2 warnings，4 subtests passed。
- `npm --prefix apps/web run test:e2e`：100 passed。

## 2026-06-30 后续待办更新

- 阶段 3：审核页左右分屏、章节与归档体验进一步重构仍待推进。
- 阶段 4：暗黑模式全页面精调、移动端布局、可访问性与大组件拆分仍待推进。

## 2026-06-30 审核页分屏切片计划

- 已完成多 agent 只读审计：审核页已接入 `ProjectShell` 与 `StageNav`，剩余高收益入口是把三类审核分支从单列长流调整为桌面左右分屏。
- 本批只做审核页主体布局：左侧审核材料，右侧 Agent 追踪与审核决策；移动端继续单列。
- 同步给 `ProjectShell` 增加默认不变的 `maxWidth` prop，审核页使用 `lg` 宽度，其它页面默认不变。
- 不改 `/review?id=` 路由，不改审核 API、恢复接口、状态机、结果页或归档页。
- 设计文档：`docs/superpowers/specs/2026-06-30-review-split-and-rag-workspace-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-review-split-and-rag-workspace-warning.md`。
- 当前状态：本批已按 TDD 完成实现与验证；本 active 仍不归档。

## 2026-06-30 审核页分屏切片实施结果

- `ProjectShell` 增加默认不变的 `maxWidth` prop，审核页单独使用 `lg` 宽度，其他页面默认保持 `md`。
- `TaskReviewClient` 新增本地 `ReviewSplitLayout`，提供 `review-split-layout`、`review-main`、`review-aside` 稳定测试定位。
- 大纲审核、章节审核与验证审核三类分支调整为桌面左右分屏：左侧保留审核材料，右侧集中 Agent 追踪、错误提示、模型选择、审核意见与决策按钮。
- 移动端保持单列布局，新增无横向滚动和决策区可见 E2E 覆盖。
- 本批未改 `/review?id=` 路由、审核 API、恢复接口、状态机、结果页或归档页。

## 2026-06-30 审核页分屏切片验证结果

- `npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts --project=chromium`：9 passed。
- `npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts`：18 passed。
- `npm --prefix apps/web test`：111 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e`：102 passed。
- 后端联动门禁同步通过：`uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q` 为 378 passed，1 skipped，2 warnings，4 subtests passed；`uv run --project apps/agent-runtime ruff check apps/agent-runtime/` 通过。

## 2026-06-30 后续待办更新

- 阶段 3：章节与归档体验进一步重构仍待推进。
- 阶段 4：暗黑模式全页面精调、移动端布局、可访问性与大组件拆分仍待推进。

## 2026-06-30 工作流图谱暗黑模式切片计划

- 已完成多 agent 只读审计：`WorkflowOverviewCard` 已是独立组件，但节点、边标签、画布、MiniMap 和空态仍有 `#fffaf2`、`rgba(255,250,242,...)`、`#1d2a27` 等浅色硬编码。
- 本批只做工作流图谱暗黑模式精调，保留现有节点、边、缩放、MiniMap 和工作台 Tab 语义。
- 使用 MUI theme 与 React Flow 官方 `colorMode`、`Background`、`MiniMap` 样式 props；不引入新页面风格。
- 补暗色模式 390px 工作台 E2E：图谱可见、节点不再是旧浅色背景、页面不产生横向滚动。
- 设计文档：`docs/superpowers/specs/2026-06-30-workflow-graph-dark-and-test-only-cleanup-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-workflow-graph-dark-and-test-only-cleanup.md`。
- 当前状态：本批已按 TDD 完成实现、审查与验证；本 active 仍不归档。

## 2026-06-30 工作流图谱暗黑模式切片实施结果

- `WorkflowOverviewCard` 新增 `data-testid="workflow-overview-card"`，两个 React Flow 区域补充中文 `aria-label`。
- 节点、边、边标签、React Flow 画布、背景网格、MiniMap 和无审核 Agent 空态均改为按 MUI theme mode 派生样式。
- React Flow 接入 `colorMode`，MiniMap 使用 `bgColor`、`nodeColor`、`nodeStrokeColor`、`maskColor` 官方 props。
- 新增暗色 390px 工作台 E2E，覆盖图谱可见、节点不再是旧浅色高亮、页面无横向滚动。
- 审查反馈后将节点背景断言改为 `expect.poll`，避免客户端主题切换首帧导致偶发失败。

## 2026-06-30 工作流图谱暗黑模式切片验证结果

- 红灯：`npm --prefix apps/web run test:e2e -- task-state-branches.spec.ts --project=chromium` 曾因缺少 `workflow-overview-card` 稳定定位失败。
- `npm --prefix apps/web test`：112 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e -- task-state-branches.spec.ts --project=chromium`：17 passed，审查反馈修复后已重跑。
- `npm --prefix apps/web run test:e2e`：108 passed。
- 后端联动门禁同步通过：`uv run --project apps/agent-runtime ruff check apps/agent-runtime/` 通过；`uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q` 为 380 passed，1 skipped，2 warnings，4 subtests passed。

## 2026-06-30 Markdown 暗色代码块切片计划

- 已确认本批只处理 `MarkdownContent` 的行内 code、code fence 主题感知样式，不做全站暗黑模式扫尾。
- 结果页与归档详情通过共享 Markdown 阅读链路自然受益，重点验证暗色 390px 移动端阅读器不溢出。
- 设计文档：`docs/superpowers/specs/2026-06-30-markdown-dark-code-and-graph-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-markdown-dark-code-and-graph-warning.md`。
- 当前状态：本批已按 TDD 完成实现、审查与验证；本 active 仍不归档。

## 2026-06-30 Markdown 暗色代码块切片实施结果

- `MarkdownContent` 新增主题感知行内 code 和 code fence 样式，暗色模式使用低亮度背景、边框和可读文本色，保留 `overflowX: "auto"` 与 `maxWidth: "100%"`。
- 安全链接语义保持不变：继续通过 `defaultUrlTransform` 阻断危险链接，同时允许安全的 `data:image/*` 图片用于 Markdown 图片渲染。
- `SafeLink`、`SafeImage` 处理 react-markdown 传入的 `node` 属性，避免无效属性泄漏到 DOM。
- 新增无语言 code fence 判断，避免无语言代码块混入行内 code 的边框和背景样式。
- 结果页暗色移动端 E2E fixture 增加 code fence，并断言阅读器代码块可见、背景亮度低于阈值、页面无横向滚动。
- 只读审查发现两个低风险点：E2E 背景断言过窄、无语言 code fence 混入行内 code 样式；均已修复并补测。

## 2026-06-30 Markdown 暗色代码块切片验证结果

- 红灯：`npm --prefix apps/web test -- src/components/markdown-content.test.mjs` 曾因旧 `rgba(0,0,0,0.06)` 行内 code 背景失败；审查补测后又因无语言 code fence 混入行内样式失败。
- 红灯：`npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts --project=chromium` 曾因暗色结果页 code fence 背景仍为 `rgb(245, 245, 245)` 失败。
- `npm --prefix apps/web test`：114 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts --project=chromium`：9 passed。
- `npm --prefix apps/web run test:e2e`：108 passed。

## 2026-06-30 后续待办更新

- 阶段 4：暗黑模式全页面精调、移动端布局、可访问性与大组件拆分仍待推进。

## 2026-07-01 导航玻璃态暗色切片计划

- 已完成多 agent 只读审计：`AppHeader` 仍固定浅色背景 `rgba(255, 250, 242, 0.80)`；全局 `.glass-card` 仍使用浅色 CSS 变量，`WorkflowOverviewCard` 正在使用该 class。
- 本批只做全局导航与工作流玻璃态卡片暗色精调，保留导航路由、移动端文案隐藏、工作流图谱节点/边语义和任务工作台行为。
- 补暗色 390px 首页与工作台 E2E，覆盖 header/card 不再使用旧浅色背景，并保持无横向滚动。
- 暂不处理聊天页移动端可访问性、`TaskRunClient` 实时日志区拆分或全站暗黑模式一次性扫尾。
- 设计文档：`docs/superpowers/specs/2026-07-01-header-glass-dark-and-chapter-preview-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-07-01-header-glass-dark-and-chapter-preview-warning.md`。
- 当前状态：本批已进入 TDD 实施；本 active 仍不归档。

## 2026-06-30 Markdown 暗色代码块切片计划

- 已完成多 agent 只读审计：`MarkdownContent` 行内 code 仍使用 `rgba(0,0,0,0.06)`，正文/大纲 code fence 仍使用 `grey.100`，结果页、归档详情和章节阅读器都会命中。
- 本批只做 `MarkdownContent` 代码样式主题化，保留危险链接拦截、安全图片 data URI、外链 target/rel 等安全语义。
- 补组件测试和暗色 390px 结果页 E2E，覆盖 code fence 背景不再是旧浅色、页面无横向滚动。
- 暂不处理全站 Header、图标按钮 a11y、大组件拆分和其它页面浅色命中。
- 设计文档：`docs/superpowers/specs/2026-06-30-markdown-dark-code-and-graph-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-markdown-dark-code-and-graph-warning.md`。
- 当前状态：本批计划中，待按 TDD 执行红灯与实现；本 active 仍不归档。

## 2026-06-30 章节进度面板切片计划

- 已完成并行只读审计：下一批 UI 首选工作台“章节进度”面板最小重构，直接对应阶段 3 章节体验与阶段 4 大组件拆分、可访问性。
- 本批只从 `TaskRunClient` 抽出章节进度列表与章节正文弹窗，不改任务动作、事件流、恢复流程、Tab 总体结构或 `/p/[projectId]` 路由。
- 新组件需补 `chapter-progress-panel` 稳定定位、章节按钮可访问名称、进度条可访问名称、正文弹窗标题关联、关闭按钮可访问名称和长标题断词。
- 补移动端 390px E2E：章节 Tab 可打开正文弹窗、可通过可访问名称关闭，页面不产生横向滚动。
- 设计文档：`docs/superpowers/specs/2026-06-30-chapter-progress-panel-and-sse-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-chapter-progress-panel-and-sse-warning.md`。
- 当前状态：本批已按 TDD 完成实现与验证；本 active 仍不归档。

## 2026-06-30 章节进度面板切片实施结果

- 新增 `ChapterProgressPanel`，承接工作台章节列表、章节进度、空态和章节正文弹窗。
- `TaskRunClient` 删除内嵌章节 Tab 列表与 Dialog JSX，改为传入章节状态和回调，保留原 `/chapters` 懒加载正文逻辑。
- 章节面板新增 `data-testid="chapter-progress-panel"`，章节按钮、章节进度条和关闭按钮补可访问名称。
- 章节正文弹窗补 `aria-labelledby` 与 `aria-describedby` 关联，长章节标题和按钮文本增加断词约束。
- 只读审查后修复章节列表非法嵌套 `li` 风险，并让章节正文 Dialog 在组件内常驻挂载，避免切换 Tab 时改变原弹窗生命周期。
- 因全量 E2E 暴露 390px 页面横向溢出，同步收敛 `WorkflowOverviewCard` 的 ReactFlow 容器宽度约束、最小缩放和 fitView padding。
- 本批未改任务动作、事件流、恢复流程、Tab 总体结构或 `/p/[projectId]` 路由解析。

## 2026-06-30 章节进度面板切片验证结果

- `npm --prefix apps/web run test:e2e -- task-state-branches.spec.ts --project=chromium`：16 passed，红灯阶段曾因缺少 `chapter-progress-panel` 失败。
- `npm --prefix apps/web test`：112 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e`：106 passed；首轮曾暴露 390px 横向滚动问题，修复 `WorkflowOverviewCard` 后全量通过；审查反馈修复后已重新跑过本轮全量验证。

## 2026-06-30 后续待办更新

- 阶段 3：章节与归档体验进一步重构仍待推进。
- 阶段 4：暗黑模式全页面精调、移动端布局、可访问性与大组件拆分仍待推进。

## 2026-06-30 工作台统一壳层切片计划

- 已完成多 agent 只读审计：审核页、结果页和归档详情已接入 `ProjectShell` / `StageNav`，`/p/[projectId]` 工作台仍保留本地 `Container`、面包屑和状态 Chip，项目级体验不一致。
- 本批只替换工作台页面壳层与阶段导航，不改任务动作、事件流、恢复流程、Tab 内容或 `/p/[projectId]` 路由解析。
- 新增纯函数 `resolveWorkspaceStageNav()`，用单测覆盖任务状态到“创建、规划、审核、创作、完成”五阶段的映射。
- `TaskRunClient` 已加载 workspace 分支接入 `ProjectShell`，顶部 meta 展示任务 ID、创作模型和审核模型，状态 Chip 迁移到 actions。
- 补 `task-state-branches.spec.ts` 壳层断言和 390px 移动端无横向滚动测试，并用 `task-workspace-actions.spec.ts` 回归任务动作。
- 设计文档：`docs/superpowers/specs/2026-06-30-workspace-shell-and-gateway-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-06-30-workspace-shell-and-gateway-warning.md`。
- 当前状态：本批已按 TDD 完成实现与验证；本 active 仍不归档。

## 2026-06-30 工作台统一壳层切片实施结果

- `TaskRunClient` 的 workspace 已加载分支接入 `ProjectShell`，统一工作台顶部面包屑、标题、任务 ID、创作模型、审核模型与状态 Chip。
- 新增 `resolveWorkspaceStageNav()`，把工作台状态映射到“创建、规划、审核、创作、完成”五阶段，并由单测覆盖主要状态。
- 工作台顶部接入共享 `StageNav`，E2E 覆盖 `project-shell`、`stage-nav` 与当前阶段标识。
- 新增 390px 移动端工作台无横向滚动 E2E，保留 ready_for_batch 关键动作可见，并覆盖标题缺失时回退到长不可断任务 ID 的边界。
- `ProjectShell` 面包屑补充断词与收缩约束，修复长任务 ID 回退路径导致页面级横向溢出的风险。
- 本批未改 `/p/[projectId]` 路由解析、任务动作 API、事件流、恢复流程或 Tab 内容。

## 2026-06-30 工作台统一壳层切片验证结果

- `npm --prefix apps/web test -- src/features/task-run/task-run-state.test.mjs`：14 passed，红灯阶段曾因缺少 `resolveWorkspaceStageNav` 导出失败。
- `npm --prefix apps/web run test:e2e -- task-state-branches.spec.ts --project=chromium`：15 passed，红灯阶段曾因缺少 `project-shell` 失败；长任务 ID 移动端红灯阶段曾失败为 `scrollWidth=622`，修复后通过。
- `npm --prefix apps/web run test:e2e -- review-result-archive.spec.ts --project=chromium`：9 passed。
- `npm --prefix apps/web run test:e2e -- task-workspace-actions.spec.ts --project=chromium`：5 passed。
- `npm --prefix apps/web test -- src/features/task-run/task-run-state.test.mjs src/components/stage-nav.test.mjs`：17 passed。
- `npm --prefix apps/web test`：112 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` 警告。
- `npm --prefix apps/web run test:e2e`：104 passed，最终轮次覆盖长任务 ID 移动端工作台用例。

## 2026-06-30 后续待办更新

- 阶段 3：章节与归档体验进一步重构仍待推进。
- 阶段 4：暗黑模式全页面精调、移动端布局、可访问性与大组件拆分仍待推进。

## 2026-07-01 导航玻璃态暗色切片实施结果

- `AppHeader` 增加 `data-testid="app-header"`，背景、边框、导航 active/hover 状态改为按 MUI theme mode 派生。
- 全局 `.glass-card` 增加系统暗色兜底，避免暗色系统偏好下继续使用浅色玻璃变量。
- `WorkflowOverviewCard` 使用 theme mode 回补应用内暗色背景、边框和阴影，不再只依赖 CSS 媒体查询。
- 首页移动端暗色 E2E 覆盖 header 背景 alpha 与亮度，工作台暗色 E2E 覆盖工作流玻璃卡片背景，并保持无横向滚动。
- 只读审查反馈后将工作流卡片测色断言改为 `expect.poll`，避免客户端主题水合首帧造成偶发误判。
- 本批未改导航路由、移动端文案隐藏、任务工作台动作、工作流图谱节点/边语义或事件流。

## 2026-07-01 导航玻璃态暗色切片验证结果

- 红灯：目标 E2E 曾因 `app-header` 定位缺失失败；后续暗色测色断言暴露主题水合竞态并已修复。
- `npm --prefix apps/web run test:e2e -- home-create.spec.ts task-state-branches.spec.ts --project=chromium`：22 passed。
- `npm --prefix apps/web test`：114 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` warning。
- `npm --prefix apps/web run test:e2e`：108 passed。
- 后端联动门禁同步通过：`uv run --project apps/agent-runtime ruff check apps/agent-runtime/` 通过；`uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q` 为 385 passed，1 skipped，2 warnings，4 subtests passed。

## 2026-07-01 后续待办更新

- 阶段 4：继续推进暗黑模式全页面精调，聊天页移动端可访问性、实时日志区拆分和其它浅色残留需后续独立切片处理。
- 大组件拆分仍需结合真实页面风险分批推进，本批不归档。

## 2026-07-01 聊天页移动端可访问性切片计划

- 已完成多 agent 只读审计：`ChatClient` 的思考过程触发区当前是可点击 `Box`，缺少 button 语义、`aria-expanded`、键盘操作和焦点态；消息气泡和正文对长不可断字符串缺少完整断词兜底。
- 本批只处理聊天页消息区移动端可访问性与横向溢出：补移动端会话菜单可访问名称、思考过程按钮语义、消息区 `overflowX` 兜底、长文本断词和暗色滚动条颜色。
- 不拆 `ChatClient` 大组件，不改流式聊天协议、会话存储、模型选择、模型验证状态机或验证面板布局。
- 补 390px 移动端 E2E，覆盖长用户输入、长模型回复、长思考链、无横向滚动和键盘展开思考过程。
- 设计文档：`docs/superpowers/specs/2026-07-01-chat-mobile-a11y-and-chapter-index-write-warning-design.md`。
- 实施计划：`docs/superpowers/plans/2026-07-01-chat-mobile-a11y-and-chapter-index-write-warning.md`。
- 当前状态：本批计划已确认，准备按 TDD 执行红灯与实现；本 active 仍不归档。

## 2026-07-01 聊天页移动端可访问性切片实施结果

- `ChatClient` 移动端会话菜单按钮增加 `aria-label="打开会话列表"`。
- 思考过程触发区改为原生 button 语义，补 `type="button"`、`aria-expanded`、`aria-controls` 和焦点态，保留原折叠逻辑。
- 消息滚动区、用户/助手气泡、正文、思考链和验证元数据补 `minWidth`、`overflowX` 与长文本断词约束，滚动条颜色按 theme mode 派生。
- 聊天页移动端标题栏改为可换行工具条，验证按钮与模型选择在窄屏下可收缩。
- 模型选择项在窄屏下对长显示名和 provider 省略显示，默认模型 helper text 对长 ID 做断词，避免 320px 下撑开页面。
- 审查反馈暴露 320px 页面级横向溢出，根因为全局顶部导航移动端品牌右边距、导航 gap 和 MUI `startIcon` 默认外边距合计超过视口；已压缩移动端顶部导航图标按钮间距，并保证各导航入口触控宽度不小于 44px，桌面导航语义保持不变。
- 本批未拆 `ChatClient` 大组件，未改流式聊天协议、会话存储、模型选择、模型验证状态机或验证面板业务逻辑。

## 2026-07-01 聊天页移动端可访问性切片验证结果

- 红灯：`npm --prefix apps/web run test:e2e -- chat-settings.spec.ts --project=chromium` 曾因思考过程缺少 button 语义失败。
- 红灯：新增 320px 用例曾因页面横向溢出失败。
- `npm --prefix apps/web run test:e2e -- chat-settings.spec.ts --project=chromium`：6 passed，覆盖 Enter/Space 键盘展开折叠、长默认模型 ID、长模型显示名/provider、顶部导航 44px 触控目标和无横向滚动。
- `npm --prefix apps/web test`：114 passed。
- `npm --prefix apps/web run lint`：通过。
- `npm --prefix apps/web run build`：通过，仅保留既有 `metadataBase` warning。
- `npm --prefix apps/web run test:e2e`：112 passed。
- 后端联动门禁同步通过：`uv run --project apps/agent-runtime ruff check apps/agent-runtime/` 通过；`uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q` 为 389 passed，1 skipped，2 warnings，4 subtests passed。

## 2026-07-01 后续待办更新

- 阶段 4：继续推进暗黑模式全页面精调、实时日志区拆分和其它页面可访问性/移动端细节。
- 大组件拆分仍需结合真实页面风险分批推进，本批不归档。
