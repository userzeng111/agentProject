# 大纲内容显示缺失修复

## 用户原始诉求

修复当前 Git 内容并 amend；部分任务无法成功显示大纲。

## 当前状态

正在定位审核与归档页面的大纲数据契约和渲染分支。已发现章节计划审核页将已确认章节固定传入空数组，且审核接口的结构化总纲缺少章节计划字段；归档页也需要对缺失章节计划的历史数据提供安全展示。

## 拟定修复

1. 审核接口返回完整结构化章节计划，前端据已确认数量显示已确认批次。
2. 为历史任务的缺失章节计划保留可理解的空状态和 Markdown 回退，不让页面因数组缺失崩溃。
3. 补充接口契约与前端渲染回归测试，通过后 amend 到 `f5ce94d`。

## 实施与验证

- `ReviewResponse` 已声明并返回 `outline_batch`，避免 Pydantic 丢弃批次状态；审核接口同时返回完整 `chapter_plan`。
- 章节计划审核页按已确认数量渲染确认批次；旧接口或历史数据缺少结构化章节计划时，回退显示 Markdown 或明确说明可用性。
- 归档大纲页对缺少世界观、人物或章节计划数组的历史数据安全降级，不再因空数组渲染中断。
- 新增后端响应契约测试、章节批次审核浏览器回归和历史归档回归。
- 已通过：后端定向测试 38 项、Ruff、前端 lint、TypeScript、前端单测 205 项、审核/结果/归档 Chromium 15 项及补丁格式检查。

## 当前状态

修复与验证已完成，已按用户要求 amend 当前 UI 提交；等待用户验证和是否收口归档的指示。
