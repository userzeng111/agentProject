# 自动审核重写评分0分

## 问题标题
自动审核最后的审核和重写部分，重写分数一直是0

## 用户原始诉求
现在的自动审核中的最后的审核和重写部分，重写一直是0分，分析原因并修复。

## 当前状态：进行中

## 分析结论
- 根因A（主因）：ReviewDecision.overall_score 未写入 auto_review_trace，前端只能用子Agent均值估算
- 根因B（次要）：失败 Agent 的 to_agent_result() 将 score 强制设为 None，前端 ?? 0 拉低均值
- 根因C（重写特有）：review_chapter_pair 每次完全覆盖 trace，不累积历史

## 实施计划
1. main_graph.py — 将 overall_score 写入 trace
2. task-review-client.tsx — 前端优先使用 overall_score，排除失败 Agent
3. main_graph.py 重写循环 — 累积 trace 历史
