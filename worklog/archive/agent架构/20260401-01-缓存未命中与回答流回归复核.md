# 缓存未命中与回答流回归复核

## 用户原始诉求

用户反馈当前自己构造的一版程序中，缓存没有被命中，并质疑当前实现没有按照“问题/回答流”去实现，希望结合 git 记录复核并修复该问题。

## 当前状态

已完成真实联调复核，当前工作树中的修复已经生效，但尚未收口归档。当前结论：

- 当前工作树已经补上跨阶段问答 seed，`drafting` 不再从空历史开始。
- 同参数第二次运行可以真实命中响应缓存，命中事件会通过 SSE 和工作台返回。
- `chapter-01-history.json` 已验证包含 `q1 -> a1 -> q2 -> 当前章节请求 -> a2` 连续消息链。
- 问题核心不在“当前工作树逻辑失效”，而在“修复尚未进入已提交历史”，以及仍存在若干展示与契约层残留风险。

## 已完成排查

- 对比 `git log`、`git diff` 与当前工作树，确认缓存与多轮会话相关补丁仍停留在未提交改动中。
- 复核后端关键链路：
  - `apps/agent-runtime/app/llm/story_engine.py`
  - `apps/agent-runtime/app/graph/main_graph.py`
  - `apps/agent-runtime/app/application/task_service.py`
- 复核前端工作台展示链路：
  - `apps/web/src/features/task-run/task-run-client.tsx`
  - `apps/web/src/lib/types.ts`
  - `apps/web/src/app/page.tsx`
- 启动真实服务并完成两轮样本联调：
  - 第一次任务：完整跑通 `planning -> review -> drafting -> completed`
  - 第二次同参数任务：验证 `planning` 与 `drafting` 全章节缓存命中

## 真实联调结论

### 1. 缓存命中已真实生效

- 第二个同参数任务 `task_c5fc558a87` 在 `planning` 阶段通过 SSE 收到：
  - `event_type=cache.hit`
  - `message=planning 阶段已命中模型响应缓存：outline`
- 同任务在 `drafting` 阶段，`chapter-01` 到 `chapter-06` 均收到 `cache.hit` 事件。
- `/api/tasks/task_c5fc558a87/workspace` 返回中，`context_status.cache_hit=true`，`cache_scope=response_cache`。
- 第二个任务在缓存命中后几乎立即完成归档，说明命中链路不是“只打标记不复用”，而是确实复用了缓存结果。

### 2. 问题/回答流已经打通

- 首次任务 `task_c0036a89e8` 的文件 `context/drafting/chapter-01-history.json` 已验证包含以下链路：
  - `system`
  - `q1: 请先根据以下需求生成小说大纲`
  - `a1: story_plan JSON`
  - `q2: 人工审核意见`
  - `当前章节生成请求`
  - `assistant: chapter-01 JSON`
- 说明当前工作树不再是“每次请求彼此独立”的单轮模式，已经按跨阶段消息链继续生成。

### 3. 首次任务完整结果可复现

- 首次任务 `task_c0036a89e8` 已完整生成并归档，`/api/tasks/task_c0036a89e8/result` 可正常返回正文结果。
- 第二次任务 `task_c5fc558a87` 在命中缓存后也已快速完成。

## 本轮关键样本

- 首次完整样本任务：`task_c0036a89e8`
  - 用途：验证真实模型请求、真实问答链落盘、首次生成完整完成
- 第二次同参数样本任务：`task_c5fc558a87`
  - 用途：验证 `planning` 与 `drafting` 全链路缓存命中

## 当前风险与后续关注点

- 当前修复主要还在工作树，`HEAD` 已提交版本尚不包含完整的跨阶段 seed 修复。
- 当前 `draft_conversation_seed` 是重建出来的最小问答链，不是严格复用 planning 阶段的原始请求报文，仍有语义漂移风险。
- 工作台把“上下文缓存状态”和“响应缓存命中状态”合并在同一个 `context_status` 中，展示语义仍然偏混。
- 工作台读取缓存命中主要依赖最近事件窗口，长任务下存在“实际命中过但页面看不出”的风险。

## 下一步建议

- 把当前工作树中的修复整理为正式提交，避免下次再被误判为“缓存没有命中”。
- 再补两类增强：
  - 让 `drafting` 直接复用 planning 阶段真实消息，而不是重建 seed
  - 拆分工作台中的上下文缓存与响应缓存展示字段


## 本轮新增处理范围（2026-04-01）

用户已明确要求继续处理以下两个残留点，并允许直接进入代码修改：

- `drafting` 阶段不再使用重建的最小 seed，而是优先复用 `planning` 阶段真实消息链，降低语义漂移。
- 工作台返回结构中拆分“上下文缓存状态”和“响应缓存状态”，不再混用同一个 `context_status.cache_hit/cache_scope`。

### 本轮执行策略

- 先补失败测试，锁定上述两项行为。
- 再做后端契约调整与前端展示调整。
- 最后回归后端单测、前端类型检查与真实 HTTP 验证。

## 本轮实现结果（2026-04-01）

### 已完成

- `drafting` 阶段已改为优先复用 `planning/outline-history.json` 中的真实消息链，不再以重建的 `q1/a1/q2` 作为主路径。
- `StoryEngine` 已调整为：当存在历史消息链时，章节请求会优先使用当前 `drafting` 阶段的 system prompt，不再沿用 planning 阶段的 system prompt。
- `workspace` 返回已拆分为：
  - `context_status`：只表示上下文装配、压缩、上下文缓存
  - `response_cache_status`：只表示模型响应缓存命中
- 工作台前端已同步拆分展示“上下文缓存”和“响应缓存”。

### 新增回归覆盖

- 后端新增/更新测试共 20 项全部通过。
- 关键新增覆盖：
  - graph 能读取真实 planning history 并传入 drafting
  - story engine 在历史链存在时仍使用当前章节阶段 system prompt
  - workspace 单独暴露 `response_cache_status`
  - 响应缓存状态不再依赖最近 20 条事件窗口

### 真实 HTTP 验证

- 新样本任务：`task_1c30ba1dea`
  - `/workspace` 已同时返回 `context_status` 与 `response_cache_status`
  - `context/drafting/chapter-01-history.json` 已验证为：
    - 当前 drafting system prompt
    - planning 真实 user 消息
    - planning 真实 assistant 大纲 JSON
    - 审核 follow-up
    - 当前章节请求
- 第二次同参数任务：`task_5cc0fdbe40`
  - `/workspace.response_cache_status` 已显示
    - `stage=drafting`
    - `cache_scope=response_cache`
    - `exchange_label=chapter-06`
  - 证明新消息链对应的章节响应缓存已经在第二次任务中真实命中
