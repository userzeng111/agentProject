# 问题标题
运行时状态机阻塞只读排查 task_af31e4c58f

# 用户原始诉求
你负责排查运行时状态机阻塞，只读分析，不改代码。工作区：/home/user01/WorkSpace/AgentProject。任务：围绕 task_af31e4c58f，读取 tasklog、checkpoint 相关信息，重点确认 LangGraph 是否真的产生了 interrupt、当前 graph.get_state(config) 的 next/tasks/values 是什么、是否停在 review_outline 之后却没被 TaskService 同步。请给出：1) 复现步骤与证据文件；2) 根因判断；3) 若要修复，最小改动建议。只关注 apps/agent-runtime 运行时状态机，不要扩展到前端。你不是一个人，代码库可能有其他并行修改，不要回滚他人改动。最终请列出你读取过的关键文件路径。

# 当前状态
进行中：已确认 `task_af31e4c58f` 的任务文件未进入 `waiting_outline_review`，且本次 `/review` 400 的直接原因不是“没有 outline-history”，而是 `TaskService.get_review()` 只认 `task.pending_review` 与 `task.story_plan`。

- 任务文件证据：`tasklog/runs/task_af31e4c58f/task.json` 显示 `status=planning`、`current_unit=outline-revision`、`story_plan=null`、`pending_review=null`、`auto_review=false`。
- 事件流证据：`events.md` 中只有两次 `context.history.updated`，分别对应 `outline-history.json` 与 `outline-revision-history.json`，没有任何 `review.waiting` / `waiting_outline_review` 记录。
- API 判定证据：`TaskService.get_review()` 在 `pending_review is None` 时，还要求 `task.story_plan` 非空；否则直接抛出 `ValueError(\"当前任务还没有可审核的内容。\")`，由路由层转成 HTTP 400。
- `/resume` 判定证据：`TaskService.resume_task()` 仅允许三种 `waiting_*_review` 状态，并且要求 `pending_review` 非空；当前样本因仍是 `planning + pending_review=None`，同样会返回 400。
- 持久化关系：`outline-history.json` / `outline-revision-history.json` 仅由 `_build_exchange_callback()` 落盘，它们只表示“记录了模型对话历史”，不参与 `get_review()` 和 `resume_task()` 的状态判断，也不会自动回填 `task.story_plan` / `task.pending_review`。
- store 写回关系：只有 `_sync_result()` 命中 `__interrupt__` 分支后，才会通过 `store.set_waiting_review()` 写入 `story_plan`、`pending_review`、`waiting_outline_review` 和 `review.waiting` 事件。当前任务缺少这整组痕迹，因此更接近“工作流没有把可审核态同步到 store”，而不是“review 接口读取错了 outline-history”。
- checkpoint 证据：当前仓库 `tasklog/checkpoints.db` 中没有 `task_af31e4c58f` 的记录，只有 `probe-temp`，而 `tasklog/data.db` 里存在该任务索引。这说明当前 task 文件与本地 checkpoint 库之间没有形成可核对的一致状态证据。
