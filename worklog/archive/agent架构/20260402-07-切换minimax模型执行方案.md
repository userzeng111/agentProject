---
name: 切换 MiniMax 模型执行方案
description: 评估并执行将当前测试与运行流程切换到可用 MiniMax 模型的方案
type: project
---

# 切换 MiniMax 模型执行方案

## 用户原始诉求
将当前执行改为获取到的模型列表中的 MiniMax 模型。

## 当前已知上下文
- 当前通过后端 `/api/models` 可见的 MiniMax 模型只有一个：
  - `MiniMax-M2.7-highspeed`
- 当前需要确认的范围：
  - 只切换后续复测任务使用的模型
  - 还是同时把运行时默认模型也切到该 MiniMax 模型

## 当前状态
- 已创建问题记录
- 正在等待用户确认切换范围，暂未修改代码

## 讨论与执行更新（2026-04-02）
- 用户已明确同意继续执行，并要求后续全链路复测使用模型列表中的 MiniMax 模型。
- 已通过后端接口确认当前默认模型为 `MiniMax-M2.7-highspeed`。
- 本轮目标：
  1. 先验证网关对 `MiniMax-M2.7-highspeed` 的直连调用是否成功；
  2. 再创建新的真实任务，执行大纲打回、正文打回、全文验证打回的整链路复测；
  3. 若模型调用失败或修订阶段结构化输出异常，则立即报错停止，不做 fallback 推进。

## 当前状态
- 已进入 MiniMax 实测阶段，正在执行网关直连验证与真实链路复测。

## 验证结果（2026-04-02）
- 网关直连验证：已通过 `https://api.udcode.cn/v1/chat/completions` 成功调用 `MiniMax-M2.7-highspeed`，返回合法 JSON。
- 真实任务复测：已创建任务 `task_16ef45e7c1`，模型为 `MiniMax-M2.7-highspeed`。
- 全链路结果：
  1. 大纲审核：已打回 1 次，再次提交后通过；
  2. 正文章节对审核：第 1-2 章已打回 1 次并通过，第 3-4 章已打回 1 次并通过；
  3. 全文验证审核：已打回 1 次，进入修复阶段后再次提交并通过；
  4. 最终状态：任务已完成并归档，接口返回 `status=completed`，归档时间为 `2026-04-02T13:36:47Z`。
- 结论：MiniMax 模型下，大纲打回、正文打回、全文验证打回后的恢复链路已联通，本轮未复现“未进入修复阶段”问题。

## 当前状态
- MiniMax 全链路复测已完成，等待用户确认是否继续做代码层固化或收口归档。

## 追加核查（2026-04-02）
- 用户要求进一步确认：归档任务 `task_16ef45e7c1` 的正文内容是否已完整实现大纲对应章节目标。
- 本轮处理方式：逐章比对 `outline.md` 与 `result.md`，核查每章目标、关键线索、回收点与终局收束是否在正文中落地。
- 说明：本轮先做事实核查与差异说明，不直接修改正文或代码。
