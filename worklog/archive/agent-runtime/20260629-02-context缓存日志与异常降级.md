# context 缓存日志与异常降级收口

## 问题标题

context 缓存日志与异常降级收口

## 用户原始诉求

用户要求检查当前项目变更与对应任务，继续完成活跃任务，进行代码 review、设计实现与自动化测试后归档提交。

## 当前状态

已归档。已将 context 缓存存储抽象为协议接口，补充缓存读写、清理与多层缓存异常降级日志；同时修复旧 schema 缓存命中时 `ContextSnapshot` 校验异常导致请求硬失败的问题。

## 处理结果

- `apps/agent-runtime/app/context/cache_store.py`：新增 `CacheStore` 协议；文件缓存读写、损坏 JSON、删除失败与多层缓存异常改为记录中文日志并降级。
- `apps/agent-runtime/app/context/manager.py`：缓存读取、旧 schema 校验、无效类型与写入失败均降级为重新构建上下文，避免非权威缓存拖垮请求。
- `apps/agent-runtime/tests/test_context_manager.py`：新增旧 schema 缓存降级重建回归测试。

## 验证记录

- RED：新增旧 schema 缓存测试后，当前实现抛出 `pydantic_core.ValidationError`。
- GREEN：修复后最小用例通过。
- `uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/ -q`：340 passed, 1 skipped, 2 warnings。
- `uv run --project apps/agent-runtime ruff check apps/agent-runtime/`：通过。
