# 问题标题

工作区中间文件屏蔽与归档分析

## 用户原始诉求

分析当前工作区中的未提交内容：该屏蔽的中间文件进行屏蔽，该归档的归档，并说明 `apps/agent-runtime/app/logs/app-2026-04-30-2026-05-02` 是什么文件。

## 当前状态

- 状态：已按确认方案完成屏蔽规则、日志轮转命名修复、定向验证，准备归档并提交。
- 约束：不处理已有无关改动；`model-options.test.mjs` 与 RAG plan 文档保留为未跟踪内容，不纳入本次提交。

## 初始排查方向

- 核对当前 `git status` 中未提交与未跟踪项目的来源。
- 判断日志、pid、RAG 索引、运行产物、计划文档、worklog 文档分别应忽略、保留本地或归档。
- 定点检查 `apps/agent-runtime/app/logs/app-2026-04-30-2026-05-02` 的文件类型和来源。

## 排查结论

- `.run/`：本地启动脚本/开发服务产生的 pid 与日志目录，其中 `.log` 文件已被全局 `*.log` 忽略，但 `.pid` 文件未被忽略，导致整个 `.run/` 暴露在 `git status`。
- `apps/agent-runtime/app/logs/`：后端运行日志目录，当前共约 5.1M。`app-2026-04-30-2026-05-02` 是 `TimedRotatingFileHandler` 轮转出的 uvicorn access 日志，文件无 `.log` 后缀，所以没有被现有 `*.log` 规则忽略。
- `Data/rag/`：RAG 生成索引目录，包含 `index.faiss`、`metadata.sqlite3`、`status.json`，属于可重建运行数据；当前 `.gitignore` 只有小写 `data/`，Linux 下不会匹配大写 `Data/`。
- `exampleIndexData/`：示例索引原始文本目录，当前含 `斗罗大陆.txt`，不宜默认纳入主仓库提交。
- `embeddingProject/`：这是一个独立 Git 仓库，远端为 `git@github.com:userzeng111/embeddingProject.git`，不是普通中间目录；根仓库需要在“忽略 / 子模块 / 去掉嵌套 git 后纳入 monorepo”三种方案中明确选择。
- `apps/web/src/lib/model-options.test.mjs`：前端模型过滤测试文件，不是中间产物；应判断是否属于某个未提交功能修复。
- `docs/superpowers/plans/2026-04-11-agentproject-rag-integration.md`：实现计划文档，不是运行中间产物；应判断是否作为历史计划保留或归档到对应 worklog。
- `AGENTS.md`、`CLAUDE.md`、`recovery.py`、`test_recovery_cancelled_task.py` 是已有未提交改动，不属于本次中间文件屏蔽范围，不能自动丢弃或忽略。

## 建议处理方案

1. 更新 `.gitignore`：新增 `.run/`、`apps/agent-runtime/app/logs/`、`Data/rag/`、`exampleIndexData/`。
2. 可选修正日志命名：调整 `apps/agent-runtime/app/observability/logger.py` 的轮转命名，避免未来再生成无 `.log` 后缀的 `app-YYYY-MM-DD-YYYY-MM-DD` 文件。
3. 对 `embeddingProject/` 单独决策：若作为独立项目保留，则根仓库忽略；若作为主仓库子项目，应改为子模块或移除嵌套 `.git` 后纳入主仓库。
4. 对 `model-options.test.mjs` 和 RAG plan 文档单独决策：不建议加入忽略规则。

## 已执行处理

- `.gitignore` 新增 `.run/`、`Data/rag/`、`exampleIndexData/`、`apps/agent-runtime/app/logs/`、`embeddingProject/`。
- 后端日志轮转命名新增 `_rotate_log_filename()`，将 `app-2026-04-30.log.2026-05-02` 规范为 `app-2026-04-30-2026-05-02.log`，保证未来轮转文件可被 `*.log` 兜底忽略。
- 新增 `apps/agent-runtime/tests/test_observability_logger.py` 覆盖日志轮转命名。

## 验证记录

- RED：`timeout 30s nice -n 10 uv run --project apps/agent-runtime pytest apps/agent-runtime/tests/test_observability_logger.py -q`，修复前因缺少 `_rotate_log_filename` 失败。
- GREEN：同一测试修复后结果 `1 passed`。
- Ruff：`timeout 30s nice -n 10 uv run --project apps/agent-runtime ruff check apps/agent-runtime/app/observability/logger.py apps/agent-runtime/tests/test_observability_logger.py`，结果通过。
- 忽略规则：`git check-ignore -v` 已确认 `.run/`、`Data/rag/`、`apps/agent-runtime/app/logs/`、`embeddingProject/`、`exampleIndexData/` 均命中新增规则。
