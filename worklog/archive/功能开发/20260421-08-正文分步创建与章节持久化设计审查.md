# 正文分步创建与章节持久化设计审查

- 问题标题：正文分步创建与章节持久化设计审查
- 用户原始诉求：只读审查设计文档 `/home/user01/WorkSpace/AgentProject/docs/superpowers/specs/2026-04-21-batched-chapter-generation-and-persistence-design.md`，重点检查 SQLite 结构化状态 + 本地 md 双轨持久化是否闭环、大纲审核通过后不自动开写是否自洽、目标总章节数与浮动范围关系、恢复策略、必要索引或状态字段是否遗漏。
- 当前状态：已归档。设计审查结论已全部回灌到正式设计文档，并已被后续实现与测试验证吸收。

- 审查结论：该设计文档已具备实现依据，可以定稿并进入实现计划。
- 主要缺口集中在：双写一致性顺序与补偿、章节数硬规则、状态机闭环、恢复最小字段。
- 关键问题：
  - 双写顺序、幂等键、补偿、冲突优先级未明确到可执行级别。
  - `target_chapter_count / chapter_count_min-max / planned_chapter_count` 只描述了方向，没有写死校验、越界处理与冲突规则。
  - 状态机与恢复树只覆盖了主干分支，缺少对 `created / rejected / failed / partial-draft / file-missing` 等异常路径的闭环定义。
  - 最小恢复字段不足，至少还需要补齐批次幂等与文件一致性判定所需字段。

- 整改结果：
  - 正式设计文档已新增 `continue_request_id`、`content_hash`、`artifact_state`、`persisted_count`、`attempt_no` 等关键字段。
  - 已明确采用“先建批次、再文件落盘、再回写章节状态、最后切换批次审核态”的双写顺序。
  - 已补齐 `target_chapter_count / chapter_count_min-max / planned_chapter_count` 的计算公式、越界处理与最终准绳。
  - 已补齐 `ready_for_batch / batch_generating / waiting_chapter_review / waiting_verification_review / waiting_manual_action` 等稳定状态与恢复判定树。
  - 已补齐 `effective_count` 闭环、活动批次原子认领与 `claim_token` 租约规则、审核后产物漂移的阻塞与修复回迁路径。
