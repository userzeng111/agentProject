# 问题标题

小说 RAG 全链路测试

## 用户原始诉求

对当前小说 RAG 能力执行全链路测试，要求覆盖功能实现与真实效果；如果不能达到应有的 RAG 和 LLM 效果，需要继续优化。

## 当前状态

已完成：已启动前后端并完成项目接口冒烟、全量 API 自检，以及真实 RAG 建库与问答验证；未发现新的阻断失败点。

## 验证结果

- 前后端冒烟：
  - `python3 .agents/skills/project-interface-smoke/scripts/run_smoke.py --frontend-url http://127.0.0.1:3001 --backend-url http://127.0.0.1:8010`
  - 结果：全部通过
- 全量 API 自检：
  - `python3 .agents/skills/api-full-test/scripts/run_full_test.py --backend-url http://127.0.0.1:8010 --frontend-url http://127.0.0.1:3001`
  - 结果：`21 通过 / 2 警告 / 0 失败`
  - 警告项为 created 状态任务访问 `review/result` 返回 `400`，属于脚本定义中的预期行为
- 代码级验证：
  - 后端：`./.venv/bin/pytest -q` → `66 passed in 4.62s`
  - 前端：`npm run build` → 成功
- 真实 RAG 验证：
  - `GET /api/settings/rag` 返回 `available = true`
  - 小说专用库存在：
    - `index.faiss`
    - `metadata.sqlite3`
    - `status.json`
  - 开启 RAG 真实问答：
    - 问题：`斗罗大陆的主角是谁？只回答名字。`
    - 返回：`唐三`
  - 使用归档小说私有内容验证：
    - 问题：`逆天系统里系统精灵叫什么名字？只回答名字。`
    - 开启 RAG 返回：`小灵`
    - reasoning 明确引用：`archive/task_c56c7f2dcb/artifacts/final.md`

## 当前结论

- 小说 RAG 全链路已打通：
  - 设置页建库
  - 后端消费小说专用库
  - 聊天问答命中小说语料并交给远程 LLM 生成
- RAG 对私有归档内容已体现出真实效果，不只是模型常识回答。
