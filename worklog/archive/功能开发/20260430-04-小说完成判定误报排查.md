# 小说完成判定误报排查

## 问题标题

小说完成判定误报排查

## 用户原始诉求

用户反馈某本小说事件流显示已完成并归档，但实际章节进度卡在第五章，正文没有全部实现完毕。要求找到为什么系统会判断小说已经完毕。

## 当前状态

已修复并验证，等待用户确认是否收口归档与提交。

## 处理记录

- 2026-04-30：开始按系统化调试流程排查，目标任务线索为事件流引用的 `tasklog/archive/task_1c67e121ed/result.md` 与 `result.json`。
- 2026-04-30：确认 `task_1c67e121ed` 主任务与索引状态为 `completed`，`novel_project` 记录为计划 9 章、完成 9 章、下一章 10，`chapters/index.json` 与 `chapters/01-09.json` 均存在。
- 2026-04-30：确认归档结果不完整：`result.json` 只有 4 章，`artifacts/` 只有 chapter-01 到 chapter-04 与 final；因此结果页/归档页读取 `draft_result` 时会表现为正文未完整生成。
- 2026-04-30：读取 LangGraph checkpoint 后确认工作流状态仍停留在 `completed_chapters=4`、`batch_index=4`、`draft_result=4章`；后续 5-9 章只存在于章节文件/数据库，不存在于 checkpoint 的 `completed_chapters`。
- 2026-04-30：初步根因：章节续写 `continue_task()` 走数据库与章节文件路径，未同步 LangGraph checkpoint；最后验证审核通过时 `_resume_task_sync()` 从旧 checkpoint 恢复并执行 `assemble_result()`，而 `assemble_result()` 只使用 checkpoint 中的 `completed_chapters`，不读取 `chapters/` 或数据库，导致按旧 4 章结果归档为 completed。
- 2026-04-30：补充回归测试，复现 workflow checkpoint 只有 4 章但 `chapters/` 已有 9 章时，完成归档不应只输出 4 章。
- 2026-04-30：修复完成同步逻辑：`_sync_result()` 在 `set_completed()` 前会对比计划章节数、checkpoint `draft_result` 和 `chapters/` 文件；当章节文件更完整时，用章节文件重建 `DraftResult` 与 artifacts；若结果章节数不足计划章节数，则阻止误标 `completed`。
- 2026-04-30：已修复现有误归档任务 `task_1c67e121ed`：`result.json` 重建为 9 章，`artifacts/index.json` 包含 chapter-01 到 chapter-09 与 final，归档详情接口返回 9 章，最后一章为“去成都还是留南京”。
- 2026-04-30：验证通过：`uv run pytest tests -q` 为 215 passed、1 skipped；`uv run ruff check .` 通过；`git diff --check` 通过。
