# 问题标题
章节落盘、章节审核恢复、工作台章节读取入口梳理

## 用户原始诉求
只读检查 `apps/agent-runtime/app/storage/task_store.py` 和 `apps/agent-runtime/app/application/task_service.py`，找出当前章节文件落盘、章节审核恢复、工作台章节读取的关键入口函数，给出简短结论和函数名列表，不改代码。

## 当前状态
已归档：只读梳理已完成，结论已用于后续章节落盘、恢复校验与工作台读取改造。

## 结论摘要
- 章节落盘关键入口：`_build_progress_callback()`、`_write_chapter_file()`、`_write_artifacts()`
- 章节审核恢复关键入口：`recover_task()`、`_recover_task_from_stable_state()`、`_recover_chapter_review()`
- 工作台章节读取关键入口：`get_current_chapters()`，总入口为 `get_workspace()`
