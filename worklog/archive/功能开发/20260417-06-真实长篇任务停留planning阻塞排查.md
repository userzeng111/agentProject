# 问题标题
真实长篇任务停留 planning 阻塞排查

# 用户原始诉求
继续排真实阻塞。

# 当前状态
已完成：真实长篇任务停留 `planning` 的阻塞已修复，正式任务链路已恢复并通过多轮真实验证。

# 当前结论
- 真实任务 `task_af31e4c58f` 并不是已经成功进入 `waiting_outline_review` 后 API 取不到，而是根本没有把 `pending_review + story_plan + waiting_outline_review` 同步到 store。
- 任务级 `auto_review=false` 当前会被 `TaskService._initial_state()` 中的 `task.auto_review or self.auto_review` 覆盖，导致本应进入人工审核的任务被强制带进自动审核链。
- 当前环境未安装 `langgraph.checkpoint.sqlite`，`build_graph()` 会静默退回 `MemorySaver`。服务重启后，LangGraph 中间态无法恢复，旧任务会停留在 `planning` 落盘快照。

# 修复方向
- 修正任务级 `auto_review` 的优先级：显式设置优先于全局默认值，未设置时才回退到全局。
- 为 LangGraph 增加 SQLite checkpoint 标准依赖与跨实例持久化回归测试，避免服务重启后丢失待审核状态。

# 执行结果
- 已将 `TaskCreateRequest.auto_review` 和 `TaskRecord.auto_review` 调整为可空布尔，并在 `TaskService` 中统一解析任务级与全局默认值。
- 已新增 SQLite checkpoint 依赖声明，并在本地虚拟环境安装 `langgraph-checkpoint-sqlite` 完成验证。
- 已补两条回归测试：
  - 任务级 `auto_review=false` 必须覆盖全局默认值。
  - `checkpoint_db_path` 提供时必须支持跨 graph 实例恢复状态。

# 验证结果
- 定向红绿测试通过：
  - `tests/test_task_service_workspace.py::TaskServiceWorkspaceTests::test_task_level_auto_review_false_must_override_global_default`
  - `tests/test_graph_checkpoint_persistence.py`
- 相关后端回归通过：22 项测试全部通过。
- 真实联调通过：
  - 临时服务 `127.0.0.1:8018`
  - 修复验证任务 `task_868c800c1e`
  - 显式 `auto_review=false` 时，任务成功进入 `waiting_outline_review`
  - `/api/tasks/task_868c800c1e/review` 返回 `200`
  - 新 checkpoint 可读到 `story_plan`、`normalized_spec`，`next=('review_outline',)`
- 正式重跑通过：
  - 已重启正式服务 `127.0.0.1:8017` 到当前代码
  - 正式重跑任务 `task_58b7879a8e`
  - 5 秒内进入 `waiting_outline_review`
  - `/api/tasks/task_58b7879a8e/review` 返回 `200`
- 第二条正式长篇验证通过：
  - 正式任务 `task_228f183adf`
  - 输入要求至少 6 章，实际大纲扩展为 8 章
  - 完整链路最终状态为 `completed`
  - 实际批次序列：
    - `batch_index=0`，`len=2`
    - `batch_index=2`，`len=1`
    - `batch_index=3`，`len=1`
    - `batch_index=4`，`len=1`
    - `batch_index=5`，`len=1`
    - `batch_index=6`，`len=1`
    - `batch_index=7`，`len=1`
  - 验证审核阶段得分 `78`，问题数 `8`，放行后成功收口完成
