# LLM 网关 504 恢复失败修复

## 问题标题

LLM 网关 504 恢复失败修复

## 用户原始诉求

当前任务需要人工处理；运行失败为调用聊天补全失败，状态码 504，响应为 nginx Gateway Time-out。请求摘要为“玄幻大陆废材逆袭”。用户要求分析问题出现原因，必须一次性解决问题。

## 当前状态

已修复并验证，等待用户确认是否收口归档与提交。

## 处理记录

- 2026-04-30：开始按系统化调试流程排查，先收集任务日志和代码路径证据，不直接猜测修复。
- 2026-04-30：确认任务 `task_7c8344f860` 在生成第 4 章时遭遇 LLM 网关 504；主任务记录中的 `story_plan`、`pending_review`、`draft_result` 均为空，但历史快照中存在修订后大纲和第 1-3 章正文。
- 2026-04-30：根因分为两部分：其一，外部网关 504 发生在长章节流式生成期间；其二，恢复逻辑没有在缺少 `novel_project` 和 `pending_review` 时从历史大纲与连续章节快照重建“可继续创作”状态。
- 2026-04-30：补充回归测试，覆盖从历史快照恢复到第 4 章、流式已开始后中断不重放非流式请求、章节生成使用低于 K2.6 catalog 默认值的阶段级 `max_tokens`。
- 2026-04-30：实现恢复修复：从 `outline-revision-history` / `outline-history` 与连续 `chapter-XX-history` 重建 `story_plan`、章节文件、`novel_project` 和 `novel_outline_chapter`，恢复到 `ready_for_batch` 并记录下一章。
- 2026-04-30：实现 LLM 调用修复：流式收到正文或思考内容后若中断，抛出可识别异常并禁止非流式重放；流式启动前失败仍允许 fallback；章节阶段按目标字数传入有限 `max_tokens`。
- 2026-04-30：已对实际任务执行恢复，当前 `task_7c8344f860` 状态为 `ready_for_batch`，已恢复 3 章，下一章为第 4 章。
- 2026-04-30：验证通过：`uv run pytest tests -q` 为 214 passed、1 skipped；`uv run ruff check ...` 通过；`git diff --check` 通过。
