# 问题标题

运行中小说任务模型调用异常排查

## 用户原始诉求

检查当前正在运行中的三个小说任务状态，判断系统是否存在问题；用户选择的是 K2.6，但观察到实际调用像是 MiniMax，需要分析原因。

## 当前状态

- 状态：只读排查中，尚未修改代码或配置。
- 约束：先确认任务状态、模型选择链路和实际调用证据，再讨论是否需要修复。

## 初始排查方向

- 通过后端 dashboard/workspace API 获取当前运行任务列表和每个任务的 `model_id`、`default_model_id`、`last_action_model_id`、`last_action_kind`。
- 查看任务最近事件与上下文历史中记录的模型字段，确认实际传入模型是否为 K2.6 或 MiniMax。
- 核对模型目录、默认模型、运行时协议覆盖和动作级模型选择逻辑，判断是否是展示字段误导、默认模型回退、动作模型未传递，还是网关侧别名/路由导致。

## 只读排查结论

- 后端健康检查正常，`/api/health` 返回 `ok`。
- 当前 dashboard 只显示 2 个运行中任务，不是 3 个：
  - `task_55d9909eba`（潮痕）已从生成状态进入 `ready_for_batch`，第 1-3 章已生成，当前批次审核已通过，等待继续创作。
  - `task_748f80bb68` 当前已从 `planning` 推进到 `drafting`，正在生成第 1 章，当前单元为 `chapter-01`，动作模型字段均为 `K2.6`。
  - `task_99fe24c204` 仍停在 `planning`，最近可见稳定事件为 `outline-revision`，动作模型字段均为 `K2.6`，但还没有可审核内容。
- MiniMax 调用来自自动审核链路，不是正文/大纲生成模型被替换：
  - 日志显示 `动态审核开始: review_type=outline_review, model=MiniMax-M2.7-highspeed`。
  - `task_99fe24c204` 的大纲自动审核评分为 `41.0`，`approved=False`，随后 K2.6 执行了 `outline-revision`，但仍未推进到可审核或正文阶段。
  - `task_748f80bb68` 经多轮大纲审核与修订后，最近一次自动审核评分为 `62.0`，`approved=True`，之后进入第 1 章生成。
- 可疑系统问题：
  - 自动审核默认使用 MiniMax，与用户选择 K2.6 的创作模型是两套配置，前端/工作台没有清晰区分“创作模型”和“审核模型”，容易造成误判。
  - `task_99fe24c204` 卡在 `planning` 且 `allowed_actions` 为空，未暴露恢复/继续动作；如果后台已经没有后续日志推进，应视为运行态卡住。
  - 工作区 `recent_events` 的排序不是严格时间序，且任务详情接口与 dashboard 的摘要字段不完全一致，存在状态展示不一致风险。
