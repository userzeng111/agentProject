# 问题标题
web 独立 dev 启动排查

## 用户原始诉求
不改代码，检查 `apps/web` 独立执行 `npm run dev -- --hostname 127.0.0.1 --port 3000` 是否能正常启动，并确认是否存在环境或端口依赖导致首页不可访问。输出：
1. 根因
2. 最小启动命令
3. 如果需要用户本地执行的修复动作

## 当前状态
已完成只读排查：
- `apps/web` 独立执行 `npm run dev -- --hostname 127.0.0.1 --port 3000` 可以正常启动。
- 首页可访问，问题不在前端应用代码本身。
- 后续确认真正的后台启动异常来自根目录 `start.sh` 缺少稳定的 detached 模式，已在独立问题单中修复。

## 排查计划
- 独立启动 `apps/web` 的开发服务并抓取首个错误或成功日志。已完成
- 核对 `3000` 端口占用、Node/npm 环境与前端环境变量文件。已完成
- 验证前端首页是否可访问，以及是否依赖后端 `8000` 可用。已完成
