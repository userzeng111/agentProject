# 问题标题
`main_graph.py` 章节数量与范围约束只读排查

# 用户原始诉求
只读排查后端两个评审点，不改代码。请检查 `apps/agent-runtime/app/graph/main_graph.py` 里以下问题：
1. `planned_chapter_count` 作为 `total_chapters` 使用时，是否在进入章节循环前校验与 `story_plan.chapter_plan` 长度一致；如果不一致，实际会如何在 `generate_chapter_pair / accumulate_chapters / route_after_accumulate` 之间表现。
2. 章节范围硬约束函数是否错误绕过 `target_chapter_count/chapter_count_min/chapter_count_max`，并导致 `long` 默认 `400` 被改成 `401+`。
要求给出精确文件/函数/行附近位置、真实根因、最小修复建议和建议测试点，不改代码。

# 当前状态
进行中：已开始只读核查 `main_graph.py`、`story_engine.py`、`domain/models.py`、`task_service.py`，尚未输出最终结论。
