# 前端聊天无障碍响应式修复

## 用户原始诉求

用户要求作为并行修复 worker，仅在前端聊天/无障碍/响应式和前端测试范围内修改：
`apps/web/src/lib/api.ts`、`apps/web/src/features/chat/chat-client.tsx`、`apps/web/src/components/app-header.tsx`、`apps/web/src/features/task-run/task-run-client.tsx`、`apps/web/package.json`、必要的 `apps/web/src/**/*.test.mjs`。

需先为 `streamChat` 的 fetch 网络失败、`chat.error` SSE message 处理、聊天 select 无效 value、移动端 header aria-label 或 Tabs scrollable 添加/调整最小测试，然后实现。不要改后端/CI，不要提交 git。完成后列出修改文件、测试命令和结果。

## 当前状态

已归档：用户已明确要求“收口归档 + git 提交”，实现与验证已完成并纳入提交。

## 处理记录

- 已补充 `streamChat` SSE 解析与网络失败文案的最小 `node --test` 测试，并确认测试先失败后通过。
- 已补充聊天模型选择器 value 兜底的最小 `node --test` 测试，并确认测试先失败后通过。
- 已将 `chat.error` 事件解析抽到纯函数 helper，支持 `message`、`error`、`detail` 字段。
- 已为 `streamChat` 的 `fetch` 网络异常增加 `onError` 回调，主动取消仍走 `onDone(null)`。
- 已修正聊天模型 select value，避免使用不存在或非 gateway 模型。
- 已为移动端 header 图标导航补充 `aria-label`。
- 已按 MUI v5 文档确认并为任务运行页 Tabs 添加 `variant="scrollable"`、`scrollButtons`、`allowScrollButtonsMobile` 与 `aria-label`。

## 验证结果

- `node --test apps/web/src/lib/stream-chat-events.test.mjs apps/web/src/features/chat/model-selection.test.mjs`：通过，8 个测试通过。
- `npm test`（目录 `apps/web`）：通过，23 个测试通过。
- `npx tsc --noEmit`（目录 `apps/web`）：通过，退出码 0。
- `npm run build`（目录 `apps/web`）：通过。
- `npm run lint`（目录 `apps/web`）：通过，0 warning。
