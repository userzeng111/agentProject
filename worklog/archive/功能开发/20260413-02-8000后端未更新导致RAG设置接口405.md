# 问题标题

8000 后端未更新导致 RAG 设置接口 405

## 用户原始诉求

点击“全量重建索引”按钮时返回 `{"detail":"Method Not Allowed"}`，要求停掉当前运行实例并修复。

## 当前状态

已完成验证，待收口：当前机器上不再有 `127.0.0.1:8000` 的旧实例，实际运行中的后端为 `127.0.0.1:8017`，对应最新代码，`RAG` 设置接口已实测正常。

## 本轮验证结果

- 进程核对：
  - 当前监听中的后端实例为 `apps/agent-runtime/.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8017`
  - 未发现 `:8000` 端口仍在监听，说明此前“旧实例占用 8000”现象当前已不存在。
- 路由实测：
  - `GET http://127.0.0.1:8017/api/settings/rag` 返回 `200`
  - `POST http://127.0.0.1:8017/api/settings/rag/rebuild` 返回 `200`
- 重建结果：
  - `success=true`
  - `scanned_files=7`
  - `indexed_documents=1405`
  - 索引源已包含：
    - `/home/user01/WorkSpace/AgentProject/exampleIndexData/*.txt`
    - `/home/user01/WorkSpace/AgentProject/tasklog/archive/*/artifacts/final.md`
    - `/home/user01/WorkSpace/AgentProject/tasklog/archive/*/outline.md`

## 当前结论

- 当前代码与当前运行实例上，RAG 设置页的全量重建接口已经可用。
- 若后续再出现 `405`，优先排查是否再次连接到了旧端口或旧进程，而不是接口实现缺失。
