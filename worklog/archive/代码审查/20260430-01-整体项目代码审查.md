# 整体项目代码审查

## 问题标题

整体项目代码审查

## 用户原始诉求

现在你对我现在的整体项目进行code review，分析里面存在的问题

## 当前状态

已归档：用户已明确要求“收口归档 + git 提交”，代码修复与验证已完成并纳入提交。

## 讨论与审查记录

- 本次任务定位为项目问题，按规则记录到 `worklog/active`。
- 本次只做 code review 和问题分析；未获得修复确认前，不修改业务代码。
- 并行审查范围：后端 API/服务/持久化、LangGraph/LLM/RAG、Next.js 前端、工程配置/CI/安全。
- 阻断问题：
  - `apps/agent-runtime/.env` 已被 Git 跟踪，存在密钥泄露风险，需要立即轮换并移除跟踪。
  - `.github/workflows/ci.yml` 依赖未跟踪的 `embeddingProject`，远端 checkout 后 CI 会失败。
- 高风险问题：
  - 任务文件读取接口允许跨任务读取 `tasklog` 下文件，任务隔离不足。
  - 取消/删除任务不能真正停止后台线程，可能继续调用模型、写文件、覆盖状态。
  - RAG 上下文被二次截断到极短摘要，检索内容难以真正进入生成 prompt。
  - 全文验证只检查前 8000 字，长篇后续章节无法被验证。
  - LangGraph 取消路由可能返回 `cancel_task`，但多个条件边未映射该分支。
  - LLM 网关网络错误可能被 `UnboundLocalError` 掩盖。
  - 前端聊天流初始网络失败与 `chat.error` SSE 事件处理不完整，可能卡住或吞掉错误。
- 中低风险问题：
  - 完成归档后 SQLite `storage_state` 可能仍是 `runs`。
  - 运行时协议设置 JSON 缺少锁与原子写。
  - CI 未覆盖前端构建/测试，依赖安装未使用锁文件。
  - npm/pnpm 声明不一致，lint 脚本会进入交互初始化。
  - 移动端导航缺少无障碍名称，工作台 Tab 窄屏可能溢出。
- 2026-04-30：用户确认修复全部问题。按复杂任务要求拆分并行角色，修复过程需要保留现有特性，不回退既有功能。
- 2026-04-30：已完成修复：
  - 移除 `apps/agent-runtime/.env` 的 Git 跟踪并补充环境文件忽略规则；本地 `.env` 文件保留。
  - 修复 CI 后端依赖安装、ruff 范围和前端构建/测试覆盖，移除未跟踪 `embeddingProject` CI 依赖。
  - 修复任务文件读取跨任务越界、取消/删除竞态、取消后迟到回调写入、工作流取消分支映射。
  - 修复归档后 SQLite `storage_state` 同步、运行时设置 JSON 锁与原子写。
  - 修复 RAG 命中选择、章节恢复 RAG batch_size、llama embedder 并发使用锁、RAG 引用截断、全文验证 8000 字截断。
  - 修复 LLM 网关网络错误被 `UnboundLocalError` 掩盖的问题。
  - 修复前端聊天 SSE/network 错误处理、无效模型选择值、移动端导航无障碍名称、工作台 Tab 窄屏滚动、前端 lint 生成目录误扫与 Hook 警告。
- 2026-04-30：已完成验证：
  - 后端：`uv run pytest tests -q`，210 passed，1 skipped。
  - 后端：`uv run ruff check .`，All checks passed。
  - 前端：`npm test`，23 passed。
  - 前端：`npx tsc --noEmit`，通过。
  - 前端：`npm run build`，通过。
  - 前端：`npm run lint`，通过。
  - 全局：`git diff --check`，通过。
  - 安全：`git ls-files apps/agent-runtime/.env` 无输出，且本地 `.env` 仍存在。
- 2026-04-30：仍需人工处理：由于密钥曾经进入 Git 跟踪，必须吊销并轮换已暴露的 `LLM_API_KEY`；代码侧只能移除当前跟踪，无法清除既有泄露风险。
