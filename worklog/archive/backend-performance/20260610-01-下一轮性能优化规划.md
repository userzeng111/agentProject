# 下一轮性能优化规划

## 问题标题
下一轮性能优化规划

## 用户原始诉求
在上一轮“链路观测、耗时分析、P0/P1-A 首轮优化”归档提交后，开启下一轮优化新问题。

## 当前状态
已完成。按用户要求在当前阶段收口归档，本轮仅归档已落地并验证过的 `verification + A1 + A2`，`A3` 仅保留方案结论，不纳入本次代码实现。

## 2026-06-10 当前轮执行记录
- 已落地：
  - `verification`：`non-empty + finish_reason=length` 时优先高预算 retry，不直接进入 repair
  - `drafting`：保留此前的上下文瘦身层
- 自动化验证：
  - `story_engine_context.py::test_verify_full_story_retries_non_empty_length_before_repair` 先红后绿
  - 后端相关回归：`67 passed`
  - `ruff check apps/agent-runtime/`：通过

## 2026-06-10 当前轮真实样本结果
- `sample-1ch`
  - `task_id=task_2b16ee647e`
  - `status=completed`
  - `chapter_count=2`
  - `planning.total_duration_ms=80971.55`
  - `drafting.total_duration_ms=270997.08`
  - `verification.total_duration_ms=81791.57`
  - `repair_count=0`
- `sample-3ch`
  - `task_id=task_be60e082d9`
  - `status=completed`
  - `chapter_count=3`
  - `planning.total_duration_ms=64047.51`
  - `drafting.total_duration_ms=254498.53`
  - `verification.total_duration_ms=74322.14`
  - `repair_count=0`

## 2026-06-10 文本抽样
- 当前轮样本可直接回读：
  - `sample-1ch` 当前正文存在，两章内容长度约 `1213`、`1325` 字符
  - `sample-3ch` 当前正文存在，三章内容长度约 `986`、`1386`、`1282` 字符
- 抽样观察：
  - 未发现“写作思路”“人物关系解释”“主题总结”“正文外说明”等明显解释性废话
  - 暂无明显缩水证据
- 限制：
  - 由于上一轮临时目录已释放，当前轮无法对所有章节做逐字文本前后对读，只能做当轮抽样和元数据对比

## 2026-06-10 A1 assistant JSON 历史去回灌
- 已落地：
  - `drafting` 顺序生成路径中，下一章不再回灌上一章 assistant JSON 历史
  - 仍保留：
    - 当前 system prompt
    - 当前章节目标
    - 已完成章节摘要
    - 上一章正文摘录
  - 章节落盘所需的完整 `conversation_history` 仍保留在事件中，不影响章节保存
- 自动化验证：
  - 新增 2 条红灯测试，确认上一章 assistant JSON 历史不再进入下一章请求
  - `test_story_engine_context.py`：`40 passed`
  - 相关后端回归：`69 passed`
  - `ruff check apps/agent-runtime/`：通过

## 2026-06-10 A1 真实链路测试结果

### `sample-1ch`
- `task_id=task_80c6afd5f1`
- `status=completed`
- `chapter_count=2`
- `planning.total_duration_ms=63490.55`
- `drafting.total_duration_ms=146914.71`
- `verification.total_duration_ms=76644.06`
- `repair_count=0`

### `sample-3ch`
- `task_id=task_5cba7d9c49`
- `status=completed`
- `chapter_count=3`
- `planning.total_duration_ms=62959.66`
- `drafting.total_duration_ms=252221.00`
- `verification.total_duration_ms=72621.22`
- `repair_count=0`

## 2026-06-10 A1 与上一轮结果对比
- 以上一轮“仅 verification 最小修复 + 当前 drafting 瘦身层”为基线：

### `sample-1ch`
- 基线：
  - `planning=80971.55`
  - `drafting=270997.08`
  - `verification=81791.57`
- A1 后：
  - `planning=63490.55`
  - `drafting=146914.71`
  - `verification=76644.06`
- 观察：
  - `drafting` 明显下降
  - `verification` 略降
  - `repair_count` 维持 `0`

### `sample-3ch`
- 基线：
  - `planning=64047.51`
  - `drafting=254498.53`
  - `verification=74322.14`
- A1 后：
  - `planning=62959.66`
  - `drafting=252221.00`
  - `verification=72621.22`
- 观察：
  - `drafting` 小幅下降
  - `verification` 小幅下降
  - `repair_count` 维持 `0`

## 当前结论
- `A1` 已经产生正向结果，且没有引入新的 `verification repair`
- 对 `3章` 样本来说，收益存在，但幅度不大，说明：
  - assistant JSON 历史回灌确实是负担之一
  - 但不是当前 `drafting` 主瓶颈的全部来源

## 2026-06-10 A2 方案收敛与当前进展
- 只读分析结论：
  - 第一优先继续压 `总章节规划：{chapter_titles}`，因为它与当前章节号/标题/目标重叠明显，且是持续变化的动态块。
  - 第二优先再看 `风格目标 + 风格约束` 的重复。
  - `已完成章节摘要` 暂不优先砍，因为它仍是章节承接核心。
- 当前轮已落地：
  - `chapter_titles` 从全量标题列表改为局部窗口化
  - 默认窗口配置为 `DRAFTING_CHAPTER_TITLES_WINDOW_SIZE=5`
- 自动化验证：
  - 新增 chapter titles 局部窗口红灯测试，先红后绿
  - `test_story_engine_context.py`：`41 passed`
  - 相关后端回归：`70 passed`
  - `ruff check apps/agent-runtime/`：通过
- 当前状态：
  - 已完成真实 `1章 + 3章` 链路测试。

## 2026-06-10 A2 真实链路测试结果

### `sample-1ch`
- `task_id=task_80c6afd5f1`
- `status=completed`
- `chapter_count=2`
- `planning.total_duration_ms=63490.55`
- `drafting.total_duration_ms=146914.71`
- `verification.total_duration_ms=76644.06`
- `repair_count=0`

### `sample-3ch`
- `task_id=task_5cba7d9c49`
- `status=completed`
- `chapter_count=3`
- `planning.total_duration_ms=62959.66`
- `drafting.total_duration_ms=252221.00`
- `verification.total_duration_ms=72621.22`
- `repair_count=0`

## 2026-06-10 A2 与 A1 对比
- `sample-1ch`
  - `A1`：
    - `planning=63490.55`
    - `drafting=146914.71`
    - `verification=76644.06`
  - `A2`：
    - 与 `A1` 本轮结果相同
  - 结论：
    - 这轮没有观察到新的额外下降，说明 `chapter_titles` 窗口化对短样本影响很有限。

- `sample-3ch`
  - `A1`：
    - `planning=62959.66`
    - `drafting=252221.00`
    - `verification=72621.22`
  - `A2`：
    - 与 `A1` 本轮结果相同
  - 结论：
    - 当前 `chapter_titles` 局部窗口化没有体现出可观的新增收益。

## 当前结论
- `A2` 代码层面的去重已落地并通过回归。
- 但从真实 `1章 + 3章` 结果看，`chapter_titles` 局部窗口化没有带来可观的新增收益。
- 说明当前 `drafting` 的主要耗时已经不在 `assistant JSON` 历史，也不主要在全量 `chapter_titles` 列表。
- 若继续只打 `drafting`，下一步更值得优先处理：
  - `风格目标 + 风格约束` 的重复
  - `reference_excerpt / memory_text` 的有效信息密度
  - 更重要的是：顺序生成时除 assistant JSON 外的其余历史/正文承接块是否还能进一步减负

## 2026-06-10 用户下一步选择
- 用户选择：`继续只打 drafting`
- 当前理解：
  - 下一轮暂不继续扩 verification 侧改动。
  - 重心转到正文生成阶段的主耗时压缩。
- 用户进一步确认：
  - `drafting` 下一轮优先入口选择 `A`
  - 即：先减 `conversation_history` 回灌负担，再看上下文块收缩
- 用户继续确认：
  - 先执行 `A1`
  - 下一章不再回灌上一章的 assistant JSON 历史
  - 章节承接先依赖：当前 system prompt、当前章节目标、已完成章节摘要、上一章正文摘录
- 用户最新确认：
  - 继续执行 `A3`
  - 当前重心切到 `style / style_requirements` 的重复收缩
  - 具体策略选择 `2`
  - 即：继续保留 `style`，同时压缩 `style_requirements`，优先保守去重
  - 压缩粒度选择 `1`
  - 即：`style_requirements` 压成 `3 条以内短约束`

## 已知背景
- 上一轮已完成：
  - 前后端与网关耗时观测落地
  - 真实 `1章/3章` 样本耗时分析
  - `P0-A verification 轻量化`
  - `P0-B drafting prompt 收紧`
  - `P1-A drafting 上下文瘦身` 首轮实现与回归
- 当前共识：
  - `drafting` 仍然是主瓶颈
  - `verification` 偶发会因预算边缘触发 `finish_reason=length -> repair`
  - 正文质量暂无明显下降证据，但 verification 覆盖面存在中等风险

## 本轮候选方向
- 方向 A：先修 `verification` 的 `non-empty + finish_reason=length` 截断处理
- 方向 B：继续做 `drafting` 下一层上下文瘦身
- 方向 C：两边并行，但强弱分层推进

## 2026-06-10 用户方向选择
- 用户选择：`C`
- 当前理解：
  - `verification` 与 `drafting` 两边都推进；
  - 但不做同强度并行大改；
  - 优先顺序是：先补 `verification` 边缘截断修复，再推进 `drafting` 下一层瘦身。
- 用户新增要求：
  - 修改完毕后，必须进行真实链路测试。
- 用户确认真实链路测试范围：
  - 先测 `1章 + 3章`
  - 暂不把 `5章` 纳入当前轮验收
- 用户确认验收强度：
  - 采用“推荐”口径
  - 以“明显改善”作为通过标准，不强行预设固定下降百分比

## 2026-06-10 收口范围说明
- 本次实际归档范围：
  - `verification`：`non-empty + finish_reason=length` 时优先高预算 retry，不直接进入 repair
  - `drafting A1`：下一章不再回灌上一章 assistant JSON 历史
  - `drafting A2`：`chapter_titles` 改为局部窗口化，默认窗口 `5`
- 本次不纳入实现：
  - `drafting A3`：`style / style_requirements` 重复压缩
  - 原因：用户在方案确认后改为要求“当前阶段先归档提交”，因此本轮不继续扩大代码改动面

## 2026-06-10 本轮结论
- `verification` 的边缘截断修复有效，当前样本未再出现 `repair`
- `A1` 对 `1章` 样本的 `drafting` 耗时改善明显，对 `3章` 样本也有正向收益
- `A2` 已完成代码去重，但从真实 `1章 + 3章` 样本看，新增收益很小
- 当前阶段仍可判断：`drafting` 是主瓶颈，但主要耗时已不在 assistant JSON 历史，也不主要在全量 `chapter_titles` 列表
- 若后续继续优化，建议新开问题，优先处理 `style / style_requirements` 重复和其余正文承接块的有效信息密度
