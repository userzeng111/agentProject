# task_ffcc008c19 误完成与正文缺失追踪

## 问题标题

追踪任务 `task_ffcc008c19` 在没有生成正文的情况下被错误标记为已完成的完整执行链路，并修复根因。

## 用户原始诉求

用户指出该任务在整体执行流程中被误完成，实际上没有任何正文生成；要求追踪任务链并定位、修复问题。

## 当前状态

已完成根因定位与代码修复，未改写历史任务 `task_ffcc008c19` 数据，保留原始取证链。

## 根因与证据

- 原始总纲响应缺少 `planned_chapter_count` 与 `chapter_plan`；`StoryPlan` 默认值将其吞为 0 与空数组。
- 空计划仍进入验证；空正文被验证修复器接受，并合入了不存在的第 42 章补丁。
- 验证修订上限后的强制通过路径缺少正文完整性校验，最终写入 `completed`。
- 取证目录：`tasklog/runs/task_ffcc008c19/`；其中 `events.md` 没有 `chapter.started` 或 `chapter.saved`，`task.json` 的计划数为 0，`draft_result` 只有非正文的第 42 章内容。

## 已实施修复

- 大纲阶段要求有效正整数计划章节数，缺失时仅重试一次，仍缺失即失败。
- 章节计划批次必须非空、编号连续，并合入已确认总纲；未补齐全部章节计划不得进入正文。
- 正文起草、全文验证、验证修订、结果组装和服务层 `set_completed` 前均增加计划数、正文数量、章节编号与非空正文门禁。
- 验证修订不再允许空正文，且忽略不存在章节的模型补丁。

## 验证状态

- 后端相关回归通过，覆盖空计划、空正文、编号不连续、章节计划不足和完成结果校验。
- 等待用户确认后再归档或提交本问题记录。
