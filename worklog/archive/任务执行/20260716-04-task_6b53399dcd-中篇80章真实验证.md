# task_6b53399dcd 修复与中篇 80 章真实验证

## 用户原始诉求

定位并修复 `task_6b53399dcd` 当前问题；修复后运行一次中篇 80 章小说生成，并进行 UI 自动化验证。

## 当前状态

已定位失败点：第二个章节大纲批次（第 6–10 章）中，模型返回单个第 6 章对象，旧实现仅读取 `chapters` 字段而得到 `None`，从而错误转入人工处理。

已完成修复：章节批次输出改为只接受完整连续的 `chapters` 数组；单章、缺章、错序或旧缓存会触发现有 JSON 修复请求，而不是中断任务。已补充单章自动修复、无效缓存跳过的回归测试。

校验结果：章节分区、缺章恢复、批次生成、规划窗口与图路由相关回归共 96 项通过（1 项跳过），Ruff 与差异空白检查通过。已新增受环境变量保护的真实 80 章浏览器慢测；该流程会实际调用模型并保留任务及日志。

真实浏览器验证发现并修复前端竞态：模型目录先于工作台数据返回时，动作模型被过早初始化为空，导致带有效创作模型的新任务不显示“开始创作”。现改为取得工作台后再初始化，并补充“模型目录先返回”的浏览器回归测试。已创建但尚未启动的首次验证任务会清理，随后以修复后的 UI 重新创建并运行唯一的 80 章任务。

当前保留的真实任务为 `task_661e059fa3`，已准备从第 16 章缺口继续运行。

真实续跑确认第 16 章能够单独补齐，且第 17–20 章未被覆盖；但又暴露出窗口暂停被恢复器无差别自动确认，导致第 21–40 章尚无正文时越界规划了第 41–60 章。已停止并取消该无效验证任务，保留其事件日志。修复后，`window_draft_ready` 会直接落为 `ready_for_batch`，只有当前窗口正文完成后的显式恢复才可生成下一窗口；正常的 LangGraph 中断也记录为“已暂停”而非节点失败。相关后端回归 140 项通过（1 项跳过），前端 lint 通过，准备以新任务重新执行真实 UI 慢测。

新建的真实 UI 验证任务为 `task_0c85db6012`。慢测在提示词中加入每次运行唯一标识，已确认总纲、章节计划与正文调用均未命中历史缓存。该任务已真实完成第 1–20 章正文，并停在 `waiting_chapter_review`；第 21 章尚未被提前规划，验证了 20 章窗口门禁。

80 章全流程慢测尚未跑至 `completed`。用户已确认本批收口，因此本记录如实归档为“核心修复与首窗口真实验证完成”，全量慢测后续另行跟进。
