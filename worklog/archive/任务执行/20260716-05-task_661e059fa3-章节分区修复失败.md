# task_661e059fa3 章节分区修复失败

## 用户原始诉求

定位并修复 `task_6b53399dcd`，并通过 UI 自动化运行一次中篇 80 章真实小说生成。

## 当前状态

真实任务 `task_661e059fa3` 已跨过原第 6–10 章批次失败点并完成 20 章规划，正文已保存 19 章。第 16 章初次响应解析失败后，模型修复回复缺少章节分区边界，任务被错误转为 `waiting_manual_action`。正在并行定位解析兼容与 checkpoint 恢复路径，目标是在保留现有章节的前提下修复并继续自动化验证。

已确认根因：第 16 章原始回复与第一次修复回复均只有 JSON 元数据加内容边界，缺少元数据边界与结束边界；通用 JSON 修复提示又与章节分区协议冲突。正文也在结束前被截断，不能直接宽松保存。修复将为章节调用专用分区修复提示，并只在存在结束边界时兼容缺失元数据边界。另发现恢复进度错误按“文件数量”而非“连续章节号”计算，导致缺第 16 章时可能重写第 20 章；正在同步修复。

真实慢测现支持通过 `REAL_LLM_E2E_TASK_ID` 从保留任务续跑，并会在 `waiting_manual_action` 时使用工作台恢复对话框继续，不会新建重复任务。

已补充恢复进度校正：以连续正文前缀而不是已落盘文件数量确定进度。因此本任务会从第 16 章继续，仅补写第 16 章，不覆盖已经保存的第 17–20 章，并在补齐后将第 1–20 章一并进入审核。章节专用修复提示会要求模型带回同一次随机的元数据、正文、结束三条边界；截断正文仍会被拒绝，避免静默保存残章。后端回归 96 项通过（1 项跳过），正在启动真实浏览器续跑。

续跑实际完成第 16 章补齐并验证未覆盖第 17–20 章。随后发现规划窗口控制回归：等待当前窗口正文的 `window_draft_ready` 被泛化自动恢复吞掉，错误规划到第 60 章；该任务已于 2026-07-16 19:03（本地时间）取消，避免继续产生不符合窗口约束的数据。

该任务的事件日志已保留，仅作为缺章恢复与窗口越界的回归样本。对应修复及验证已由新任务完成，本记录随本批收口归档。
