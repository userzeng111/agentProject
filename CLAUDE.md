# <主目录，记录任务计划> 项目提示词

本文件适用于当前目录 `<主目录，记录任务计划>` 及其子目录。

## 1. 语言与沟通

- 所有思考、总结，回复必须使用中文描述。
- 如果是在Windows下，控制台的编码默认为utf-8，所有读取工具也需要用utf-8。
- 项目问题（与当前仓库代码、配置、流程直接相关）需先记录问题并进入讨论，不得直接改代码。
- 非项目问题（通用问答、闲聊、与仓库无关事项）不进入 `worklog`，可直接处理。
- 优先使用MCP工具，为了保证信息安全，除非MCP满足不了，才使用其他的。
- 需求不明确有疑问的地方向我询问澄清，提供预定义选项与我交流讨论，给出选择选项，即将完成请求前必须向我询问，必须向我询问并得到可以完成任务/结束时，禁止主动结束对话/请求。
- 代码中的注释、报错信息信息这些也全部用中文。

- 复杂任务必须自动使用 Agent Teams 团队模式或者subagent模式工作，先分析好需要的角色，然后并行启动，当前任务结束后自动回收。
- agent Teams的角色选用模型在没有指定的情况下，默认为当前使用的模型，模型等级由你决定。

## 2. 执行门禁

- 讨论完成后，只有用户给出明确肯定（例如“同意”“可以”“继续”“执行”）才可进入代码修改。
- 若用户表达不确定（例如“先别动代码”“再看看”），保持讨论状态，不执行修改。



## 3.Git 提交约束

- 收口执行顺序固定为：**先归档（active -> archive 并更新 `worklog/index.md` 与 `worklog/history.md`），再提交。
- 用户一次明确同意收口（例如“同意执行”“同意提交并推送”“同意”）后，自动连续执行“归档+提交”。
- `git commit` 信息必须为中文，简要描述本次变更，提交需要给出主体大纲。
- `git commit` 前必须在目标仓库目录执行并核对：
	- `pwd`
	- `git rev-parse --show-toplevel`
	- `git remote -v`
	- `git status --short`
- 若核对结果与目标仓库不一致，立即停止并先与用户确认。



## 4. 问题记录与归档

- 仅项目问题需要记录；非项目问题不记录。
- 提示词/规则文档修改不记录问题文档，不进入 `worklog/active` 与 `worklog/archive`。
- 记录必须保存在当前目录下：`<主目录，记录任务计划>/worklog/`。
- 目录结构：
	- `worklog/active/`：进行中的问题。
	- `worklog/archive/`：已完成问题。
	- `worklog/index.md`：当前未完成问题索引（active）。
	- `worklog/history.md`：历史归档问题索引（archive）。
- 分类方式：按主题分类存储。
	- 活跃问题：`worklog/active/<主题>/<YYYYMMDD-序号-标题>.md`
	- 已归档问题：`worklog/archive/<主题>/<YYYYMMDD-序号-标题>.md`
- 新问题必须新建文档，不复用旧文档。
- `worklog/active/...` 问题文档中必须至少记录：
	- 问题标题
	- 用户原始诉求
	- 当前状态
- 问题完成后必须及时归档，并同步更新 `worklog/index.md` 与 `worklog/history.md`。
- 只有当相关工作已完成，且用户明确同意收口时，才允许把 `worklog/active/...` 移动到 `worklog/archive/...`。
- 若相关工作未完成，或用户未明确同意收口，则保持在 `worklog/active/`，禁止提前归档。
- `worklog/index.md` 仅保留当前未完成问题，不包含已归档历史。
- `worklog/history.md` 仅保留已归档问题，不包含进行中问题。
- `worklog/index.md` 只保留“标题 + 路径 + 一句话摘要”。
- `worklog/history.md` 只保留“标题 + 路径 + 一句话摘要”。
- 两个索引都不得写入长过程、命令流水或大段输出。



## 5. Token 控制与上下文管理

- 不将所有历史记录堆在同一文档。
- 当前轮默认仅引用：
	- 当前问题文档（`worklog/active/...`）
	- 索引摘要（`worklog/index.md`）
- 其他文档按需读取，用到再查，不做全量回读。
- 需要查历史时，先读取 `worklog/history.md` 再按路径回查 `worklog/archive/...` 全文。



## 6. 标准流程

1. 项目问题创建问题文档并记录用户原始问题；非项目问题跳过记录。
2. 在对应的 `worklog` 文档中持续记录讨论结论、方案、影响、风险与当前状态。
3. 用户明确同意后，执行代码修改。
4. 执行过程中持续更新对应问题文档状态。
5. 仅当相关工作已完成且用户明确同意收口时，才归档问题文档，并同步更新 `worklog/index.md` 与 `worklog/history.md`。



## 7. MCP规则

3.始终使用`context7`来查询第三方工具、第三方库的最新使用文档，必须确认清楚才能去实施。
4.始终优先使用`github`来查看github上的源码、文件等等资料。

## 8. <子目录（项目目录）> 版本号规则

- 每次进入“项目代码修改”任务时，默认先将 `<子目录（项目目录）>/src/Cargo.toml` 中 `[workspace.package].version` 的最后一位（patch）`+1`。
- 只允许主动修改最后一位（patch）；主版本与次版本（major/minor）只能在用户明确要求时修改。
- 每次任务只执行一次默认 `patch +1`，不得重复累加。
- 推送时必须同步告知本次版本变化，格式为：`旧版本 -> 新版本`。

## 9. 代码修改硬约束

### 9.1 总原则（最高优先级）

- 所有已确认特性均为硬约束，必须保留。
- 允许因 upstream 更新修改代码实现，但修改后必须保证：
	- 特性完整保留；
	- 对外行为与语义不变。
- 任一特性回归即判定失败，必须停止并修复或回滚当前批次。
